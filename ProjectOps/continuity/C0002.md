# ProjectOps — continuity/C0002.md — ABOS

State: ACTIVE
Segment: C0002
Opened: 2026-09-13
Host-Branch: `abos/p006-security-audit-remediation-v2`

## Intervención — P-006 — Dependency security remediation desde main actual

State: EN_EJECUCIÓN

### Objetivo

Restaurar el gate autoritativo `security-audit` sin debilitarlo, corrigiendo los advisories vigentes de Vitest/@vitest/mocker y `stream-json`, y mantener ABOS sobre runtimes Node realmente mantenidos y compatibles con su dependency graph seguro.

### Baseline exacto

- Base de esta continuación: `main` `5f854b44d92e2f2c3381465f7f45165218d2dad1`.
- Active-Plan heredado: P-006.
- La rama histórica `abos/p006-security-audit-remediation` existe y NO se sobrescribe.
- Head histórico auditado: `42654daab559330d0acbc27f1b3b21dd03f81c69`.
- No existe PR abierto/cerrado asociado a esa rama histórica.

### Evidencia recuperada de la rama histórica

El trabajo previo no se repitió ciegamente:

1. El probe final instaló `vitest@4.1.11` en todos sus candidatos.
2. `vitest-only` falló en `pnpm audit` porque `stream-json` seguía vulnerable; ese rojo no demostró incompatibilidad funcional de Vitest 4.
3. `stream-json-patched` con Vitest 4.1.11 + override `stream-json@<=3.4.0 -> 3.6.0` pasó en Node 22: audit, smoke Solana JSON-RPC, typecheck, build y full `test:ci`.
4. Elevar Solana también pasó, pero no aportó beneficio necesario frente al override selectivo y amplía superficie de cambio.

### Ejecución actual — candidato combinado

Se creó un probe temporal reproducible sobre la base actual. La infraestructura de probe fue eliminada después de producir evidencia y no forma parte del diff final.

Run `34780233007`, job `103785781368`:
- frozen baseline install: PASS;
- aplicar Vitest 4.1.11 + stream-json patched graph: PASS;
- `pnpm audit`: PASS, sin advisories;
- smoke Solana JSON-RPC: PASS;
- typecheck: PASS;
- build: PASS;
- primer `test:security`: se detectó incompatibilidad CLI de Vitest 4 porque ABOS usaba `--grep`;
- se auditó el mismo patrón y también afectaba `test:financial`;
- ambos scripts se migraron a `--testNamePattern`;
- security tests: PASS;
- financial tests: PASS;
- full `test:ci`: PASS;
- manifests deterministas generados y commiteados como `9cc3a976db642f2cd0d25f7aa9b7c5a646cc72aa`.

### Limpieza del probe

- workflow temporal eliminado en `38ec7295a4167baf6161ae72ec0e6e3ac092cfc1`;
- smoke temporal eliminado en `881feda1457a40bb9294ee5cbc582d7040e007a0`;
- compare limpio desde base a `881feda...`: sólo ProjectOps + `package.json` + `pnpm-lock.yaml`.

### Hallazgo discriminante — Node 20 ya no es compatible con el graph seguro

CI limpio run `34780393921` sobre `881feda...`:
- ProjectOps Integrity run `34780393860`: SUCCESS;
- Node 20 (`20.20.2`) falla en `pnpm install --frozen-lockfile` antes de tests:
  `ERR_PNPM_UNSUPPORTED_ENGINE` porque `stream-chain@4.2.5` exige Node `>=22`.

La investigación upstream confirmó:
- `stream-json` está afectado hasta `3.4.0` y se corrige desde `3.5.0`;
- `stream-json@3.5.0` ya declara Node `>=22` y depende de `stream-chain ^4.2.5`;
- por tanto no existe una release upstream parcheada que preserve la antigua lane Node 20;
- Node 20 está EOL y no recibe fixes de seguridad.

Mantener Node 20 exigiría una de estas rutas peores:
- desactivar `engine-strict` y ejecutar una dependencia upstream no soportada;
- mantener una fork/patch propia que `pnpm audit` seguiría identificando por versión o que crearía deuda de supply-chain;
- aceptar/ignorar el advisory.

Las tres contradicen P-006 y la política de verdad/seguridad.

### Revisión del antiguo bloqueo Node 24

README documentaba que Node 24 se rechazó porque `better-sqlite3` 11 podía carecer del prebuild Windows necesario y caer a `node-gyp`.

Upstream actual ofrece `better-sqlite3` 13:
- migra el addon a N-API;
- declara Node `>=22`;
- incluye prebuilds empaquetados;
- elimina la razón histórica principal para bloquear Node 24.

Decisión `DECISION_READY` revisada:
- retirar Node 20 EOL;
- mantener Node 22 como default/recommended;
- añadir y validar Node 24 como segunda LTS soportada;
- elevar `better-sqlite3` a la línea 13 para que la lane Node 24 sea una capacidad real, no un engine range nominal.

### Hipótesis / falsación siguientes

- H1: `better-sqlite3` 13 conserva contratos usados por ABOS y sus DB migrations. Falsación: full suite + state/database tests en Node 22/24.
- H2: Node 24 instala prebuilds de forma suficiente en Linux/Windows. Falsación: CI Windows 22/24 + install/build/smoke.
- H3: Vitest 4 sigue estable en Node 24. Falsación: full suite/security en Node 24.
- H4: el graph final mantiene `pnpm audit` clean. Falsación: security-audit sobre lock final.

### Semántica a preservar

- no debilitar `pnpm audit` ni ignorar severidad;
- `engine-strict=true` permanece;
- no reemplazar tests para ocultar fallos;
- no elevar Solana sin necesidad demostrada;
- installer, README, package engines y CI deben contar la misma verdad;
- cambio de runtime support se basa en lifecycle + dependency evidence, no conveniencia;
- ProjectOps debe reflejar exactamente cualquier bloqueo o regresión.

### Plan de ejecución revisado

1. P-006 plan reconciliado antes de cambiar el contrato runtime.
2. Actualizar dependency graph a Vitest 4 patched + stream-json patched + `better-sqlite3` 13.
3. Cambiar autoridades de runtime de Node 20/22 a Node 22/24, preservando default 22.
4. Regenerar lockfile determinísticamente.
5. Ejecutar frozen install/typecheck/build/full/security en Node 22 y 24.
6. Ejecutar Windows 22 y 24, audit y distribución pública.
7. Si aparece regresión, aislar causa; no desactivar engines ni audit.
8. Integrar sólo con evidencia actual; revalidar main.
9. Cerrar P-006 y continuar P-003.

### Estado actual

EN_EJECUCIÓN. El supuesto Node 20/22 quedó explícitamente invalidado por evidencia. Próxima unidad: implementar y validar contrato Node 22/24 + better-sqlite3 13 sobre el graph patched.
