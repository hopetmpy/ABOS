# ProjectOps — continuity/C0002.md — ABOS

State: ACTIVE
Segment: C0002
Opened: 2026-09-13
Host-Branch: `abos/p006-security-audit-remediation-v2`

## Intervención — P-006 — Dependency security remediation desde main actual

State: EN_EJECUCIÓN

### Objetivo

Restaurar el gate autoritativo `security-audit` sin debilitarlo, corregir los advisories vigentes de Vitest/@vitest/mocker y `stream-json`, y mantener ABOS sobre runtimes Node mantenidos y compatibles con su dependency graph seguro.

### Baseline exacto

- Base de esta continuación: `main` `5f854b44d92e2f2c3381465f7f45165218d2dad1`.
- Active-Plan heredado: P-006.
- La rama histórica `abos/p006-security-audit-remediation` se auditó como evidencia y no se sobrescribió.
- Head histórico auditado: `42654daab559330d0acbc27f1b3b21dd03f81c69`.

### Evidencia recuperada y no repetida ciegamente

1. `vitest@4.1.11` ya había sido ejercitado en probes previos.
2. `vitest-only` había fallado por el advisory de `stream-json`, no por incompatibilidad de Vitest.
3. Vitest 4.1.11 + override selectivo de `stream-json` había pasado Node 22, Solana JSON-RPC smoke, typecheck, build y full suite.
4. Elevar Solana también había pasado, pero no era necesario para el advisory y ampliaba superficie.

### Hallazgos nuevos de esta continuación

#### Vitest 4

- Vitest 4 retiró `--grep` del CLI.
- `test:security` y `test:financial` todavía usaban esa sintaxis.
- Se migraron a `--testNamePattern`.
- Security, financial y full tests pasaron después de la migración.

#### Node 20

Run `34780393921` demostró que Node `20.20.2` ya no puede instalar el graph seguro: `stream-chain@4.2.5`, dependencia de la línea parcheada de `stream-json`, exige Node `>=22`.

La línea parcheada mínima de `stream-json` también exige Node `>=22`. Node 20 está EOL. Mantenerlo requeriría ignorar engines, aceptar el advisory o mantener una fork/patch propia. Esas rutas se descartaron.

Decisión: retirar Node 20 y validar Node 22/24, manteniendo Node 22 como default/recommended.

#### Node 24 / better-sqlite3

Se falsaron dos candidatos antes de encontrar uno compatible:

1. `better-sqlite3@13.0.3` — run `34781052095`, job `103788015717`: fresh install Windows Node 24 cayó a `node-gyp rebuild` y falló por toolchain. Se descartó como distribución normal de este release; no se añadió Visual Studio/C++ como workaround.
2. `better-sqlite3@12.12.0` — run `34783482130`, job `103794622330`: `ERR_PNPM_NO_MATCHING_VERSION`; existía evidencia upstream de release pero no publicación utilizable en npm. Se descartó.
3. `better-sqlite3@12.11.1` — última 12.x publicada auditada; declara Node 22/24 y conserva ruta prebuilt. Se adoptó como candidato final.

### Regeneración determinista final

Run `34783641140`, job `103795047158`, base `f40b64e61916b1cc98e5fcc7a65480fc68cb0775`: SUCCESS.

PASS:
- `pnpm install --no-frozen-lockfile`;
- resolved dependency graph inspection;
- `pnpm audit --prod=false`;
- typecheck;
- build;
- security tests;
- financial tests;
- full `test:ci`;
- deterministic lockfile commit `7d5dd14922c18e64cf63552e2372eeb569f15db4`.

El workflow temporal de regeneración se eliminó después de producir la evidencia. No forma parte del diff final.

### Clean branch validation

Clean SHA: `721c484e3ba11dfc9876ca7de330183c23f9c4e7`.

CI run `34783793081`: **SUCCESS**.

PASS en el mismo SHA:
- build-and-test Node 22;
- build-and-test Node 24;
- security-audit;
- windows-regression Node 22;
- windows-regression Node 24;
- public-distribution-smoke Node 22;
- public-distribution-smoke Node 24;
- rebrand-integrity.

ProjectOps Integrity run `34783793072`: **SUCCESS**.

Windows 22 y 24 acreditaron fresh frozen install, typecheck, build, CLI/state smoke y portability regressions. Node 24 ya no requiere la compilación/toolchain adicional que hacía fallar el candidato v13.

### Mejoras de evidencia en CI

- Las matrices 22/24 usan `fail-fast: false` para preservar evidencia independiente por lane. Un fallo sigue haciendo fallar el workflow; sólo deja de cancelar la otra lane.
- `public-distribution-smoke` ahora clona anónimamente el ref público actual en branches/PRs y `main` en `main`, evitando falsos resultados por probar código distinto del SHA bajo validación.

### Contrato técnico preparado para integración

- Node: `>=22 <23 || >=24 <25`.
- default/recommended: Node 22.
- Vitest: `^4.1.11`.
- `better-sqlite3`: `^12.11.1`.
- override: `stream-json@<=3.4.0 -> 3.6.0`.
- `engine-strict=true` permanece.
- `@solana/web3.js` no se eleva sin necesidad.
- README, installer, tests y CI se están reconciliando con 22/24.

### Semántica preservada

- no se debilitó `pnpm audit`;
- no se ignoró severidad;
- no se eliminaron tests para ocultar fallos;
- no se exigió toolchain C++ al usuario para la ruta normal;
- no se cambió Solana sólo para corregir una transitiva;
- UNKNOWN/fallo de una hipótesis produjo una ruta nueva, no retry ciego;
- release upstream no se confundió con versión publicada en registry;
- branch PASS todavía no equivale a integración en `main`.

### Siguiente unidad

1. Reconciliar documentos/baseline que todavía mencionen Node 20/22.
2. Revalidar ProjectOps/CI del head documental final.
3. Comparar contra `main`, abrir PR P-006 y verificar exact head/diff.
4. Integrar sólo con gates verdes.
5. Revalidar el SHA de `main` resultante.
6. Sólo entonces marcar P-006 HECHO y avanzar directamente a P-003 / PR #29.

### Estado actual

EN_EJECUCIÓN / INTEGRATION_READY a nivel de source+branch evidence. No HECHO todavía: falta integración y validación de `main`.
