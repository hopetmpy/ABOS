# ProjectOps — continuity/C0005.md — ABOS

State: ACTIVE
Segment: C0005
Opened: 2026-09-13
Host-Branch: `abos/p009-authority-provenance-v1`

## Intervención — P-009 — Corregir Authority, Provenance y trust boundaries end-to-end

State: EN_EJECUCIÓN
Classification: IMPLEMENTED / VALIDATION_PENDING

### Objetivo

Reconstruir y corregir la cadena de autoridad y provenance desde ingress hasta policy/tool execution para impedir que contenido, relabeling, forwarding o persistencia eleven autoridad sin autenticación/evidence correspondiente.

### P-xxx relacionado

P-009.

### Baseline exacto

- Base de apertura: `main` `1a2a548276ec47289c77b2085c5c939c6bca0019`.
- P-008: HECHO, integrado por PR #34.
- P-008 main CI `34797078874`: SUCCESS.
- P-008 main ProjectOps Integrity `34797078882`: SUCCESS.
- Rama: `abos/p009-authority-provenance-v1`.
- Cutover P-008→P-009: `b49170bfd7d33c197cd278d127ba8a416021d781`.
- Cutover CI `34797573671`: SUCCESS.
- Cutover ProjectOps Integrity `34797573650`: SUCCESS.
- Decisión registrada: `d0560f413020b0c98d6c9e844549e3f850638df0`.
- Baseline documental previo a source: CI `34798293364` SUCCESS; ProjectOps `34798293370` SUCCESS.
- Source productivo implementado: `d5976ee6a89ba13e552cf95842fca410a87fccf8`.

### Required-Context y superficies auditadas

Autoridades y contexto obligatorios leídos/reconstruidos antes de decidir:
- `AGENTS.md`;
- `ProjectOps/system/ABOS_OPERATING_PROTOCOL.md` completo;
- `ProjectOps/system/ABOS_ADAPTIVE_REASONING_LAYER.md` completo;
- `ProjectOps/CONTINUITY.md` / C0004;
- `ProjectOps/PROJECT.md`;
- `ProjectOps/PLAN.md`;
- `ProjectOps/plan/P-008.md`;
- `ProjectOps/plan/P-009.md`;
- `constitution.md`;
- ingress/consumer relevantes de `src/agent/`;
- `src/replication/messaging.ts`, lineage/constitution y parent-child authority paths;
- `src/orchestration/messaging.ts` y `colony-task-assignment.ts` porque Colony es el forwarding real consumido por replication/orchestrator;
- `src/social/client.ts`, `signing.ts`, `protocol.ts`, `validation.ts` como producer real de inbox;
- `src/registry/` para separar discovery/registry identity de transport authority;
- `src/state/schema.ts` y `src/state/database.ts` para persistencia/restart;
- tests de inbox/social/authority/policy como evidencia existente.

### Semántica preservada

- parent != child;
- transport principal != asserted sender;
- content/payload no puede autoelevar autoridad;
- identity UNKNOWN permanece unknown/untrusted;
- forwarding conserva provenance material;
- policy/constitution/auth no se reemplazan por un nuevo sistema paralelo;
- no se almacenan secretos innecesarios en eventos públicos;
- source/CI no acredita autenticación externa LIVE no ejercitada;
- rutas abiertas de ABOS no implican trust abierto;
- el Social relay actual no se promociona ficticiamente a verificación criptográfica end-to-end cuando el payload de `poll()` no entrega una firma verificable al receptor.

### Grafo observado

1. `SocialClient.send()` firma outbound payloads.
2. `SocialClient.poll()` autentica al receptor frente al relay para polling, pero el mensaje recibido tipado por ABOS contiene `from/to/content/...` y no una firma inbound verificable.
3. El `from` de `poll()` es por tanto una **aserción del transporte/relay**, no una identidad criptográficamente verificada por ABOS.
4. `check_social_inbox` sanitizaba y persistía `from/content`, pero `inbox_messages` v14 no conservaba transport, verification level ni provenance.
5. `runAgentLoop()` reclamaba cualquier mensaje social conversacional y lo transformaba incondicionalmente en `pendingInput.source = "agent"`.
6. `PolicyEngine.deriveAuthorityLevel()` clasifica `creator` y `agent` como authority `agent`.
7. `authority.external_tool_restriction` bloqueaba herramientas destructivas para source external/heartbeat; tests existentes demuestran que `agent` sí puede atravesar esa frontera.
8. Structured Colony messages se excluyen del loop conversacional, pero `parseInboundMessage()` validaba el `message.from` interno sin ligarlo al `InboxMessage.from` exterior.
9. `createColonyTaskAssignmentConsumer()` autoriza `task_assignment` comparando ese `message.from` interno contra `parentAddress`; sin binding previo un payload podía autoafirmar el parent.
10. Registry/discovery ofrece identity metadata y on-chain ownership, pero no es transport-auth authority para inbox y permanece separado.
11. Existe `verifyMessageSignature()` canónico en `src/social/protocol.ts`; se reutilizará cuando exista una firma inbound verificable. P-009 no creó un segundo verificador ni inventó evidencia ausente.

### Hipótesis competidoras — resolución

H1 — **CONFIRMADA**: existía una ruta externa real `social relay → inbox → source="agent" → PolicyEngine` donde un mensaje no autenticado end-to-end por ABOS alcanzaba authority de agente.

H2 — **FALSADA para inbox conversacional**: el relabel no era sólo presentación. `inputSource` alimenta directamente la derivación de authority y las reglas de policy.

H3 — **CONFIRMADA**: provenance se perdía en persistencia y en Colony el sender interno no se ligaba al sender exterior antes de autorizar parent/child.

H4 — **CONFIRMADA**: ya existían contratos parciales suficientes (`InboxMessage`, `InputSource`, `AgentTurn`, `AgentMessage`/MessageEnvelope, DB inbox). La ruta correcta fue REUSE/EXTEND/UNIFY; no se creó un segundo identity registry o message authority.

Hipótesis lateral P010-H1 — **NO ELEVADA a defecto runtime principal**: `executeTool()` sólo evalúa policy si existen `policyEngine && turnContext`, pero el entrypoint principal crea `SpendTracker` y lo pasa al loop, por lo que el runtime canónico sí entrega `turnContext`. La hardening de policy lifecycle/API interna pertenece a P-010 salvo reproducción adicional.

### Decisión aplicada

Clasificación dominante: **EXTEND + UNIFY + CORRECT + MIGRATE**.

No CREATE de una authority paralela.

Implementación aplicada:
1. `InputSource` incorpora `external`; policy conserva backward compatibility de `undefined/heartbeat` como external.
2. `InboxMessage` y DB conservan provenance mínimo abierto: `transport`, `senderVerification`, `transportSender`.
3. Social produce `social_relay / relay_asserted`; LocalDB produce `local_db / local_trusted`.
4. schema v15 migra aditivamente inbox/turns; filas legacy quedan `legacy_unknown / unknown`.
5. `AgentTurn.inputProvenance` conserva mensaje, sender afirmado/observado y transformaciones relevantes.
6. inbox conversacional sólo deriva `agent` cuando todo el lote proviene de `local_db/local_trusted`; social/legacy deriva `external`.
7. Colony liga `message.from/to` interno con sender/recipient exterior persistido antes de handlers/parent authorization y falla cerrado ante mismatch.
8. no se atribuye verificación criptográfica inbound inexistente; el máximo claim Social actual sigue siendo `relay_asserted`.
9. se añadieron tests adversariales y se actualizaron fixtures manuales v14 para el contrato v15 sin debilitar las pruebas de mismatch.

### Evidencia de implementación dirigida

Primer intento one-shot `34799157518`: **FAIL controlado**.
- aplicación del patch: PASS;
- typecheck: PASS;
- 7/7 pruebas adversariales nuevas: PASS;
- fallo restante: fixture histórico de `orchestration/messaging.test.ts` construía manualmente una tabla v14 sin columnas v15 y usaba identidades inner/outer incoherentes;
- no alcanzó commit/push, por tanto no dejó source productivo parcial.

Segundo intento one-shot `34799290285`: **SUCCESS**.
- Node `22.23.2`;
- patch guardado: PASS;
- fixture reconciliado: PASS;
- `pnpm run typecheck`: PASS;
- targeted tests: **6/6 files PASS, 159/159 tests PASS**;
- `authority-provenance.test.ts`: 7/7 PASS;
- commit productivo: `d5976ee6a89ba13e552cf95842fca410a87fccf8`;
- helpers one-shot eliminados en el mismo commit y ausentes del HEAD final productivo.

### Auditoría de diff neto

Comparación `d0560f41...` → `d5976ee6...`:
- ahead 3 / behind 0;
- 10 archivos netos, todos dentro del alcance P-009;
- sin workflow/script temporal en el diff neto;
- sin drift lateral detectado.

Archivos netos:
- `src/types.ts`;
- `src/state/schema.ts`;
- `src/state/database.ts`;
- `src/social/client.ts`;
- `src/agent/loop.ts`;
- `src/agent/policy-rules/authority.ts`;
- `src/orchestration/messaging.ts`;
- `src/__tests__/authority-provenance.test.ts`;
- `src/__tests__/authority-rules.test.ts`;
- `src/__tests__/orchestration/messaging.test.ts`.

### Estado de validación

El head productivo `d5976ee6...` fue creado por `github-actions[bot]`. Sus gates estándar posteriores aparecen `action_required` antes de runner por política de actor; eso se clasifica **BLOCKED_PRE_RUNNER / ACTOR_APPROVAL_POLICY / NO VERIFICADO**, no source FAIL.

Esta reconciliación documental se realiza mediante una mutación autorizada/humana para producir un nuevo head estable y volver a ejecutar CI + ProjectOps completos. P-009 permanece **EN_EJECUCIÓN** hasta que rama/PR/main satisfagan la evidencia de integración.

### Ataques / validación cubierta y pendiente

Cubierto por tests dirigidos:
- spoofed inner `from` no atraviesa Colony;
- external→agent relabel eliminado en la proyección de inbox;
- inner `to` mismatch rechazado;
- legacy provenance UNKNOWN permanece external/untrusted;
- migration v14→v15 conserva filas y no inventa trust;
- persist/reload conserva provenance de inbox/turn;
- parent/child envelope consistente sigue procesándose;
- `external` explícito activa la restricción de herramientas peligrosas.

Pendiente antes de HECHO:
- CI completo + ProjectOps Integrity sobre un SHA humano exacto de rama;
- PR #35 exact-head/mergeability/reviews;
- integración protegida en `main`;
- CI + ProjectOps sobre el `main` integrado;
- cierre/rotación de continuidad sólo después de esa evidencia.

### Impactos evaluados

- Directo: `types.ts`, social adapter, inbox persistence, loop, policy source classification, Colony parser.
- Upstream: Social relay y LocalDB transport producers.
- Downstream: PolicyEngine, authority rules, tool boundary, turn audit trail.
- Lateral: replication/orchestration parent-child messaging; registry queda NO_CHANGE como transport authority.
- Temporal: restart conserva provenance; legacy rows degradan a UNKNOWN.
- Persistente: migration aditiva de inbox/turns, sin reescritura destructiva.
- Operativo: mensajes sociales arbitrarios pierden privilegio de agente; child structured messages deben demostrar consistencia entre envelope interno y sender observado.

### Límites

- Clone/container local: NO DISPONIBLE por DNS observado previamente; no se repite ciegamente.
- GitHub connector + GitHub Actions: DISPONIBLE / AUTORIZADO.
- Ninguna credencial, identidad privada o estado `~/.abos` se escribe en ProjectOps.
- La confianza efectiva que el backend relay aplique a firmas outbound es una frontera externa no inspeccionada aquí; P-009 no la inventa ni la eleva a E5.
- Social inbound sigue siendo `relay_asserted` hasta que un transporte entregue evidencia verificable suficiente para usar el verificador canónico.

### Siguiente punto verificable

Obtener CI completo + ProjectOps Integrity sobre el nuevo head humano de reconciliación. Si ambos quedan verdes, auditar PR #35 exact-head y proceder a integración protegida. No cerrar P-009 hasta revalidar `main`.
