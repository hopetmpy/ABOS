# ProjectOps — continuity/C0013.md — ABOS

State: ACTIVE
Segment: C0013
Opened: 2026-09-22
Host-Branch: `abos/p017-capability-acquisition`

## Intervención — P-017 — discovery, acquisition, composition y construction de capacidades

State: EN_EJECUCIÓN
Classification: HECHO_SOURCE / E3_BRANCH_GREEN / MAIN_INTEGRATION_PENDING
Decision-State: DECISION_READY
Decision-Class: EXTEND / REUSE / UNIFY

## Baseline y authority

- P-016 product merge: `08b7a5cce8f18c57c52e6a2f43048b6f1c214e50`.
- baseline CI `35809162263`: SUCCESS.
- baseline ProjectOps `35809162403`: SUCCESS.
- P-014 `CapabilityRegistry` + `CapabilityStore` permanece única authority genérica de readiness.
- P-012 permanece authority de construction/self-mod transaccional.
- P-015 permanece authority de runtime MCP.
- P-013 permanece authority de correlación/evidence transversal.
- P-017 orquesta esas authorities; no crea un registry, runtime MCP, self-mod runner ni evidence store paralelos.

## Hallazgos que justificaron cambio

1. `CapabilityResolver` clasificaba gaps (`use_existing`, `change_environment`, `probe`, `blocked`, `acquire`, `compose`, `construct`, `unknown`) pero no ejecutaba remediation end-to-end.
2. `resolve_capability` era discovery/planning, no acquisition.
3. Installers/configuradores existentes no acreditaban runtime usability ni readiness.
4. P-015 ya poseía discovery/call MCP real y lifecycle evidence-backed; duplicarlo era incorrecto.
5. P-012 ya poseía candidate isolation, gates, activation CAS, rollback/recovery; construction debía delegarse allí.
6. Skills ya aportaban runtime truth parcial mediante re-probe de requirements.
7. No existía una supply-chain authority que permitiera inferir confianza de un candidato externo sólo por nombre/instalación; ausencia de provenance/assessment debía fallar cerrado.
8. P-013 podía registrar intentos/resultados P-017 sin asumir capability authority.
9. No existía un acquisition/composition/construction coordinator P-017 equivalente al abrir la intervención.

Hipótesis discriminadas:
- `NO_CHANGE`: FALSADA.
- `EXTEND_EXISTING_CAPABILITY_FABRIC`: ACEPTADA.
- `CREATE_PARALLEL_ACQUISITION_MANAGER`: RECHAZADA.
- `REUSE_EXISTING_INSTALLERS_AS_CAPABILITY_AUTHORITY`: FALSADA.

## Arquitectura implementada

### Coordinator y lifecycle

`src/capabilities/acquisition.ts` implementa:

`CapabilityRequest -> resolución inicial -> discovery -> candidate provenance/assessment -> route -> canonical registration/probe -> re-resolve del contrato original -> outcome`.

Propiedades verificadas:
- provenance + assessment explícitos antes de efectos externos;
- budget ceiling cuando existe coste conocido y rechazo honesto de coste no demostrable;
- deduplicación por route/candidate;
- no blind retry después de outcome incierto;
- registro sólo a través de P-014;
- éxito nominal de install/config/build nunca equivale a VERIFIED;
- `NO_DISCOVERED`, `UNKNOWN`, `UNAVAILABLE`, `UNAUTHORIZED`, `PROHIBITED`, `DEGRADED` y `VERIFIED` permanecen distintos;
- un candidato materialmente diferente puede seguirse tras rechazo de otro;
- Evidence Fabric recibe intentos/resultados sin convertirse en readiness authority.

### Runtime wiring

- `remediate_capability` permanece separado de `resolve_capability`.
- nested operations reingresan por `executeTool -> PolicyEngine` mediante `protected-tool-invoker`.
- dynamic/external tools no pueden shadowear silenciosamente nombres internos ABOS; duplicados ambiguos dentro de la misma trust class fallan cerrado.
- outer remediation persistence redacta nested probe/construction/composition values sensibles; los argumentos reales sólo llegan a la inner protected execution.

### MCP route

`src/capabilities/routes/mcp-functional-probe.ts` reutiliza P-015:
- sólo usa una capability MCP ya descubierta/proyectada;
- exige argumentos funcionales reales cuando son necesarios;
- ejecuta el tool MCP a través de su Policy boundary propia;
- re-resolve P-014 decide el resultado final;
- no inventa no-op arguments ni promoción por retorno nominal.

### Construction route

`src/capabilities/routes/p012-construction.ts` reutiliza P-012:
- source edits pasan por la authority transaccional existente;
- un transaction success sólo materializa estado adquirido/probe-pending en el proceso actual;
- source escrito no se promociona como capability usable sin reload/probe/evidence posterior.

### Composition lifecycle

`src/capabilities/routes/verified-composition.ts` + `src/capabilities/composition-tool.ts` implementan:
- plan declarativo durable sin runtime secret values;
- mínimo dos componentes y steps explícitos;
- sólo dependencies P-014 execution-ready;
- cada step debe estar cubierto por el contrato `provides` de los componentes declarados;
- functional probe y uso pasan por protected nested execution;
- persistencia del plan ligada a hash/version integrity;
- dependencia degradada o desaparecida degrada el composite;
- el executor vuelve a comprobar en cada ejecución que los contratos actuales de los mismos componentes todavía cubren cada `step.toolName`; contract drift no puede producir un silent provider/tool switch.

## Adversarial validation

Tests añadidos cubren, entre otros:
- existing capability short-circuit;
- no candidate / discovery failure;
- provenance o assessment incompletos;
- budget ceiling;
- install/build sin false VERIFIED;
- verified sólo después de evidence-backed probe/re-resolution;
- dependency no ready;
- unknown-effect sin retry ciego;
- alternate candidate;
- Evidence lifecycle;
- protected nested Policy re-entry;
- internal tool shadowing/collision;
- MCP functional route;
- P-012 construction handoff;
- nested sensitive-argument redaction;
- persistent composition lifecycle;
- composition dependency/contract drift.

## Gates exactos

Checkpoint temprano:
- core head `f1b9a8431afc317b5083cf885cc654a012b1de41`;
- CI `35815824274`: SUCCESS, 8/8;
- ProjectOps `35815824292`: SUCCESS.

Checkpoint previo a hardening final:
- head `5090876b9bad69a255114798071f96c2a1a9076f`;
- CI `35817655824` tuvo un único timeout transitorio en `windows-regression (22)` sobre un test P-016 byte-identical a `main`; Node22/24, Windows24, security y public distribution pasaron;
- rerun exacto del job Windows22 `107323769559`: SUCCESS sin cambio de P-016, por lo que no se atribuye causalmente a P-017 ni se modifica código sano.

Hardening final:
- `7e74e26ef356338a6a5359d43391ef6106c42836` — runtime composition contract-coverage revalidation;
- `6f6bd342ecaddaa1ca55ca4e8cdbf40bd0b8957c` — regression test de contract drift;
- exact-head CI `35903430155`: SUCCESS, 8/8 (Node 22/24 full+security, Windows 22/24, public distribution 22/24, dependency audit/rebrand);
- exact-head ProjectOps `35903430064`: SUCCESS.

Evidence level actual: E3 branch. No se eleva a E5/LIVE.

## Estado técnico y frontera restante

Objetivo técnico P-017: SATISFECHO EN RAMA.

No se marca canónicamente HECHO todavía porque faltan:
1. PR/integración exacta en `main`;
2. CI + ProjectOps del merge exacto en `main`;
3. transición canónica a P-018 después de esa evidencia.

No quedan defects HARD conocidos de P-017 abiertos en este checkpoint. Cualquier nuevo fallo se discrimina causalmente antes de modificar source.

## Siguiente punto verificable

1. Abrir PR de `abos/p017-capability-acquisition` hacia `main`.
2. Validar exact PR head y ausencia de review/thread blockers materiales.
3. Integrar mediante el método canónico del repositorio.
4. Revalidar exact `main` con CI + ProjectOps.
5. Marcar P-017 HECHO sólo con esa evidencia y activar P-018 en un segmento nuevo.
