# ProjectOps — continuity/C0015.md — ABOS

State: CLOSED
Segment: C0015
Opened: 2026-09-23
Closed: 2026-09-23
Host-Branch: `abos/p019-adaptive-inference`

## Intervención — P-019 — Model/Connection Fabric + Adaptive Inference

State: HECHO
Classification: E3_MAIN_GREEN / INTEGRATION_VERIFIED
Decision-State: DECISION_READY
Decision-Class: EXTEND_EXISTING_CONNECTION_MODEL_FABRIC / BASELINE_IS_SEED_NOT_LIFECYCLE_AUTHORITY / MIGRATE_LIVE_LEGACY_CONSUMERS_TO_CANONICAL_ROUTER / NO_SILENT_CROSS_PROVIDER_FALLBACK
Execution-Kernel: `AGENTS.md`

## Baseline

- P-018 final branch head: `33bbd4bb1751a7c82aa1e2a84edcf20e4724b992`.
- P-018 PR #52 squash merge: `9bf05ce5be0e5497a56b28f1584678c1f15b3994`.
- exact-main CI `35908965085`: SUCCESS.
- exact-main ProjectOps `35908964855`: SUCCESS.
- P-019 branch abrió exactamente desde ese `main` verde.

## Authority map final

- `AGENTS.md`: única authority de cadence/chaining/recovery/closure del agente de desarrollo.
- `ProjectOps/system/ABOS_OPERATING_PROTOCOL.md` y `ABOS_ADAPTIVE_REASONING_LAYER.md`: referencias non-scheduler.
- `src/ai-connections/registry.ts`: connection/auth adapter registry abierto.
- `src/inference/registry.ts`: `ModelRegistry` durable/canónico.
- `src/inference/router.ts`: routing canónico con budget, timeout, evidence y compatibilidad con conexión activa.
- `src/setup/model-picker.ts` + `src/setup/configure.ts`: discovery/selección/hot-switching y `enableModelFallback` configurable.
- `src/setup/ai-connection-adapters.ts`: adapters y metadata provider-specific; Codex expone catálogo/reasoning metadata dinámicos.
- `src/codex/inference.ts`: runtime Codex real con reasoning effort provider-native.
- `src/inference/provider-registry.ts` + `src/inference/inference-client.ts`: compatibility legacy; ya no son authority del runtime canónico y quedan para cleanup P-035.
- P-013 Evidence Fabric sigue siendo authority de telemetry/correlation; P-019 no creó otro spend/evidence ledger.

## Unidad 1 — lifecycle dinámico — HECHO

- `ModelRegistry.initialize()` trata `STATIC_MODEL_BASELINE` como seed/upsert de IDs explícitos;
- no retira modelos dinámicos same-provider por ausencia en baseline;
- preserva estado `enabled` existente;
- regresiones cubren modelo dinámico OpenAI same-provider y refresh baseline sin re-enable falso.

Evidencia:
- source head de unidad: `9093ce72e170b1aa349577396f7735c0939c6af4`;
- CI `35910102455`: SUCCESS;
- ProjectOps `35910102268`: SUCCESS.

## Unidad 2 — canonical runtime migration — HECHO

Consumer audit confirmó que `ProviderRegistry`/`UnifiedInferenceClient` estaban vivos en `agent/loop.ts`, workers y Orchestrator, creando split-brain con el `InferenceRouter` canónico.

Implementado:
1. `src/inference/orchestration-client.ts` define el contrato mínimo de planner/worker inference.
2. `src/inference/router-orchestration-client.ts` delega al `InferenceRouter` y resuelve la conexión activa en cada call.
3. Falta de connection authority => fail-closed; no provider fallback list interno.
4. `responseFormat=json_object` y `toolChoice=required` se preservan con validación fail-closed.
5. P-013 recibe `inference.orchestration_route_requested` con correlación goal/task/turn.
6. `agent/loop.ts` dejó de instanciar el fabric legacy como authority para workers/orchestration.
7. Provider/model/budget/compatibility/evidence de workers/planner pasan por authorities canónicas.
8. El workflow one-shot usado únicamente para aplicar el parche grande fue retirado; no quedó como infraestructura.

## Unidad 3 — legacy compatibility boundary clamp — HECHO

- `UnifiedInferenceClient.chat()` selecciona un único candidate antes de dispatch;
- retries sólo ocurren dentro del mismo provider;
- error retryable/non-retryable no continúa hacia otro provider;
- `chatDirect()` conserva provider/model explícitos;
- circuit state permanece client-local y no muta `ProviderRegistry`;
- provider change requiere una nueva decisión superior.

El primer gate exacto reveló cuatro tests stale y un bypass real por `registry.disableProvider()`; se corrigieron ambos y el retest completo quedó verde.

Commits finales:
- test boundary `3861d7f5908238290bd2cac42bc672dfc873f7b9`;
- circuit authority correction `e0b9e06b1239fdc074579687f98fd8744dca34c8`;
- integration regression reconciliation `2745589a74b483d5a4accb9797714cbc7a8d44c0`.

## Manual lock vs adaptive mode — NO_CHANGE

- `inferenceModel` no implica por sí solo lock; `src/setup/configure.ts` expone `Enable model fallback` por separado;
- `InferenceRouter.route()` usa `enableModelFallback`; con `false` no intenta candidato alternativo;
- con fallback habilitado, el candidate set permanece filtrado por conexión activa, tools, tier y compatibilidad;
- Codex conserva reasoning effort provider-native/configurable;
- aprendizaje de effort/latency/outcome/information-gain pertenece a P-026.

## ProjectOps kernel regression — RETEST PASS observado

Durante P-019 el usuario aportó evidencia de una cadena extensa de tools sin salida/recovery visible. `AGENTS.md` ya contenía el contrato correcto y quedó `NO_CHANGE`; se corrigió el verifier que congelaba acceptance, se alinearon referencias non-scheduler y la acceptance quedó `RETEST_PASS_OBSERVED_2026-09-23` tras una ejecución posterior con checkpoint visible. Esto no promete que app/red/plataforma nunca puedan interrumpirse físicamente.

## Adversarial review final

PASS / cubierto:
- reinicialización no borra dinámicos por ausencia en baseline;
- catálogo dinámico no salta compatibilidad conocida `false`;
- runtime canónico no usa provider registry paralelo para workers/planner;
- compatibility client no cruza provider tras dispatch ni por circuit local;
- manual lock `enableModelFallback=false` se preserva;
- timeout abortado no multiplica intentos completos;
- telemetry/cost reutiliza P-013/inference ledger existente;
- provider/model namespace permanece abierto;
- no se creó adaptive provider manager, second spend ledger ni scheduler ProjectOps adicional.

Residual explícito:
- provider/OAuth LIVE E5 y economic LIVE E6 no se acreditan por esta CI;
- retiro físico del legacy provider fabric pertenece a P-035;
- adaptive cognitive-cost learned routing pertenece a P-026.

## Evidencia final

- branch product head: `2745589a74b483d5a4accb9797714cbc7a8d44c0`;
- branch CI `35917049846`: SUCCESS 8/8;
- branch ProjectOps `35917049861`: SUCCESS;
- macro reconciliation head `b798fc8474eb59b8db18e77a12a43879718d3875`;
- reconciled CI `35917900946`: SUCCESS;
- reconciled ProjectOps `35917901158`: SUCCESS;
- PR #53 checks: CI + ProjectOps SUCCESS;
- squash merge `3e8c0eb31652175b0f3b22ff0296d52c16e758f1`;
- exact-main CI `35918621864`: SUCCESS 8/8;
- exact-main ProjectOps `35918621868`: SUCCESS.

## Cierre

P-019: HECHO / E3_MAIN_GREEN / INTEGRATION_VERIFIED para su objetivo exacto. P-020 se activa desde ese main exacto en `abos/p020-cognitive-fabric`.
