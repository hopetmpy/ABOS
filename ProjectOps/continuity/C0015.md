# ProjectOps — continuity/C0015.md — ABOS

State: ACTIVE
Segment: C0015
Opened: 2026-09-23
Host-Branch: `abos/p019-adaptive-inference`

## Intervención — P-019 — Model/Connection Fabric + Adaptive Inference

State: EN_EJECUCIÓN
Classification: DECISION_READY / FIRST_SOURCE_GATE_GREEN / CANONICAL_RUNTIME_MIGRATION_IMPLEMENTED / LEGACY_BOUNDARY_CLAMP_AUTHORIZED / PROJECTOPS_KERNEL_REGRESSION_ACTIVE
Decision-State: DECISION_READY
Decision-Class: EXTEND_EXISTING_CONNECTION_MODEL_FABRIC / BASELINE_IS_SEED_NOT_LIFECYCLE_AUTHORITY / MIGRATE_LIVE_LEGACY_CONSUMERS_TO_CANONICAL_ROUTER / NO_SILENT_CROSS_PROVIDER_FALLBACK / RESTORE_PROVEN_SINGLE_KERNEL_BEHAVIOR

Execution-Kernel: `AGENTS.md`

## Baseline

- P-018 final branch head: `33bbd4bb1751a7c82aa1e2a84edcf20e4724b992`.
- P-018 PR: #52.
- P-018 squash merge: `9bf05ce5be0e5497a56b28f1584678c1f15b3994`.
- exact-main CI `35908965085`: SUCCESS.
- exact-main ProjectOps `35908964855`: SUCCESS.
- P-018: HECHO / E3_MAIN_GREEN / INTEGRATION_VERIFIED.
- P-019 branch opened exactly from green `main 9bf05ce5be0e5497a56b28f1584678c1f15b3994`.

## Authority map

- `AGENTS.md`: única authority de cadencia, chaining, recovery y cierre visible del agente de desarrollo.
- `ProjectOps/system/ABOS_OPERATING_PROTOCOL.md`: constitución técnica reference-only; no scheduler.
- `ProjectOps/system/ABOS_ADAPTIVE_REASONING_LAYER.md`: reasoning reference-only; no scheduler.
- `ProjectOps/system/ABOS_ADAPTIVE_REASONING_ACCEPTANCE.md`: evidence report; no scheduler.
- `src/ai-connections/registry.ts`: open adapter registry for connection/auth methods.
- `src/inference/registry.ts`: durable model catalog used by current routing.
- `src/inference/router.ts`: canonical inference routing boundary for budget/timeout/evidence/connection compatibility.
- `src/setup/model-picker.ts`: discovery/manual selection/hot switching.
- `src/setup/ai-connection-adapters.ts`: provider/connection adapters; Codex already exposes dynamic official catalog and reasoning metadata.
- `src/codex/catalog.ts`: Codex-specific dynamic lifecycle/synchronization into ModelRegistry.
- `src/codex/inference.ts`: real Codex runtime and reasoning-effort execution surface.
- `src/inference/provider-registry.ts` + `src/inference/inference-client.ts`: legacy compatibility surface; already removed from the canonical worker/orchestration routing authority but still physically present for compatibility/tests until cleanup.
- P-013 Evidence Fabric remains telemetry/correlation authority; P-019 does not create another spend/evidence ledger.

## Primera unidad — lifecycle dinámico — HECHO en branch

Implementado:
- `ModelRegistry.initialize()` ahora trata `STATIC_MODEL_BASELINE` como seed/upsert de sus IDs explícitos y deja de retirar modelos dinámicos por pertenecer al mismo provider.
- se preserva el `enabled` existente de entradas baseline, evitando reactivar una selección deshabilitada explícitamente;
- regresión añadida para modelo dinámico OpenAI same-provider y refresco baseline sin re-enable.

Evidencia exacta:
- source head: `9093ce72e170b1aa349577396f7735c0939c6af4`;
- ProjectOps `35910102268`: SUCCESS;
- CI `35910102455`: SUCCESS.

Clasificación de la unidad: `HECHO / BRANCH_E3_GREEN`. P-019 global sigue `EN_EJECUCIÓN`.

## Consumer audit del fabric legacy — CERRADO

Hallazgos:
1. `ProviderRegistry`/`UnifiedInferenceClient` no eran tests-only: `src/agent/loop.ts` los instanciaba en runtime.
2. `UnifiedInferenceClient` alimentaba `createWorkerInferenceBridge()` y `Orchestrator`/planner.
3. `ProviderRegistry.resolveCandidates()` posee catálogo/tier/provider fallback independiente del `ModelRegistry`/`InferenceRouter` canónicos.
4. `UnifiedInferenceClient.chat()` agota retries de un provider y continúa silenciosamente al siguiente provider en errores retryable; tests legacy codifican expresamente ese comportamiento.
5. El main agent turn ya usa `InferenceRouter.route()` con `connectionProvider` explícito derivado de la conexión activa, por lo que existe una autoridad canónica reutilizable.
6. Workers exponen una interfaz pequeña `WorkerInferenceClient`; planner/plan-mode estaban acoplados por tipo concreto a `UnifiedInferenceClient`, no por necesidad funcional.
7. Con Codex como conexión activa, el legacy provider registry no representa la misma connection/auth authority; conservarlo como runtime paralelo producía split-brain.

## Hipótesis resueltas

- H0 `NO_CHANGE`: FALSADA.
- H1 `EXTEND_EXISTING_CONNECTION_MODEL_FABRIC`: ACEPTADA.
- H2 `CREATE_NEW_ADAPTIVE_PROVIDER_MANAGER`: RECHAZADA.
- H3 `DELETE_LEGACY_INFERENCE_NOW`: RECHAZADA — conserva compatibilidad/tests hasta cleanup P-035.
- H4 `STATIC_BASELINE_OWNS_PROVIDER_MODEL_LIFECYCLE`: FALSADA y corregida.
- H5 `RETRYABLE_MODEL_FAILURE_ALLOWS_SILENT_CROSS_PROVIDER`: FALSADA.
- H6 `ADAPTIVE_INFERENCE_BELONGS_IN_EXISTING_ROUTER_PLUS_ADAPTER_METADATA`: ACEPTADA.
- H7 `MIGRATE_LIVE_ORCHESTRATION_WORKER_CONSUMERS_TO_CANONICAL_ROUTER`: ACEPTADA e implementada en source.
- H8 `LEAVE_SILENT_CROSS_PROVIDER_BEHAVIOR_IN_COMPATIBILITY_CLIENT`: RECHAZADA — aunque ya no sea runtime authority canónica, una API compatible no debe preservar una semántica que contradice la frontera de P-019.

## Segunda unidad — canonical runtime migration — IMPLEMENTADA / GATES EN CURSO

Implementado:
1. `src/inference/orchestration-client.ts` define contrato estructural mínimo para planner/worker inference.
2. `src/inference/router-orchestration-client.ts` delega a `InferenceRouter` + runtime `InferenceClient`, resuelve `connectionProvider` vivo en cada call y no posee fallback provider list.
3. El adapter preserva `responseFormat=json_object` mediante validación fail-closed y `toolChoice=required` mediante verificación fail-closed.
4. Emite `inference.orchestration_route_requested` en P-013 con goal/task/turn correlation antes del route.
5. `agent/loop.ts` dejó de crear `ProviderRegistry.fromConfig()`/`UnifiedInferenceClient` como authority paralela para orchestration/workers.
6. Workers y Orchestrator reciben `RouterBackedOrchestrationInferenceClient`; model, budget, compatibility y evidence pasan por el `InferenceRouter` ya canónico.
7. El parche grande de `agent/loop.ts` se aplicó con workflow one-shot verificado y dicho workflow fue eliminado inmediatamente del branch.
8. Regresiones P-019 específicas añadidas para active connection dinámica, no-route sin connection authority, JSON/tool contracts y P-013 correlation.

Evidencia:
- runtime migration commit: `679f3235f6d6b769edceee2d3ef026b5a6f399e2`;
- one-shot apply run `35911542105`: SUCCESS;
- workflow temporal eliminado por `cae40f61241ac2e1aa109af7032d741c10384c18`;
- pre-kernel-intervention HEAD `aa3b3f0892447773c3d02ff80f927b765ff70a62` obtuvo checks CI/ProjectOps verdes observados en GitHub Actions antes de abrir la corrección de kernel.

## Tercera unidad autorizada — legacy compatibility boundary clamp

Objetivo: eliminar también de `UnifiedInferenceClient.chat()` la posibilidad de cambiar silenciosamente de provider después de que un provider haya sido seleccionado/intentado, para que ni siquiera callers de compatibilidad conserven esa semántica.

Reglas:
1. Un call legacy selecciona como máximo un provider/model candidate antes de dispatch.
2. Retries equivalentes pueden ocurrir sólo dentro de ese provider según la política existente.
3. Si el provider elegido falla o su circuit está abierto, el call falla con evidencia; elegir otro provider requiere un call/decision/replan nuevo.
4. `chatDirect()` conserva su frontera provider/model explícita.
5. Los tests que hoy esperan OpenAI→Groq tras 429/500/503 deben invertirse: deben probar que no existe segundo provider dispatch.
6. No se borra el módulo legacy; se corrige su semántica y queda compatibility-only hasta P-035.
7. No se rebaja evidence/cost atomicity de P-013 ni el retry budget intra-provider.

## Finding HARD — regresión del kernel operativo / comparación con ZeroIQ

Evidencia observada:
1. El usuario aportó captura donde una cadena de tool calls quedó sin salida final/recovery visible.
2. `AGENTS.md` ABOS y ZeroIQ ya contienen el mismo contrato material de `NEXT_ELIGIBLE_WORK`, `RECOVERY_IS_NOT_CLOSURE` y `STOP_GATE_REQUIRES_TERMINAL_CONDITION`; por tanto reescribir AGENTS sin evidencia sería redundante.
3. ZeroIQ ejecutó su acceptance conductual y registró 8/8 escenarios aceptados tras corregir un defecto real encontrado por la suite.
4. ABOS mantenía `Behavioral-State: BEHAVIORAL_SUITE_NOT_YET_EXECUTED` y, peor, `scripts/projectops-integrity-verify.mjs` exigía literalmente ese estado, haciendo imposible registrar una acceptance ejecutada sin romper el verifier.
5. Los documentos subordinados ABOS habían acumulado más semántica project-specific que sus equivalentes ZeroIQ, aunque `ProjectOps/PROJECT.md` ya contiene las invariantes de identidad ABOS necesarias.

Decisión:
- `AGENTS.md`: `NO_CHANGE` funcional; ya es el kernel probado y único.
- verifier: `CORRECT` para verificar el contrato probado y permitir evolución real de acceptance.
- Operating Protocol / Adaptive Reasoning: `ALIGN/SIMPLIFY` hacia el patrón estándar ZeroIQ, preservando sólo adaptación de identidad/proyecto y dejando identidad ABOS en `PROJECT.md`/`constitution.md`.
- acceptance: ejecutar/retestear conducta; no declarar PASS por documentación.
- no crear scheduler, gate, layer, authority ni documento paralelo.

Cambio aplicado hasta ahora:
- `2c09c924549c6307b0c3e68b913f0c33918a2531`: verifier alineado al single-kernel contract; ya no congela `BEHAVIORAL_SUITE_NOT_YET_EXECUTED`, protege recovery/final-output contract, artifacts retirados y wiring de branch/segment.

## Adversarial checks obligatorios

- reinicialización no puede borrar dinámicos simplemente por ausencia en baseline estático;
- catálogo dinámico no puede saltar compatibilidad/autorización de conexión activa;
- retry de modelo no puede producir cambio silencioso de provider;
- worker/planner no puede usar un provider registry paralelo cuando existe active connection canónica;
- compatibility client tampoco puede cruzar provider después de dispatch;
- adaptive mode no puede ignorar manual lock/ceiling;
- timeout total no debe multiplicarse por fallback interno;
- telemetry debe reutilizar P-013 y evitar doble contabilización económica;
- ProjectOps no puede reintroducir scheduler/layer paralelo;
- una tool-chain interrumpida no debe quedar sin recovery/final visible cuando todavía existe capacidad de responder.

## Límites de claim

- P-019 no acredita provider LIVE E5 ni economic LIVE E6 mediante mocks/CI.
- Módulos legacy no se borran hasta resolver compatibilidad/consumers; se retiran de authority/routing canónicos primero.
- Provider/model IDs permanecen datos abiertos; no se introduce allowlist central.
- Menor model size no equivale automáticamente a menor coste total.
- La corrección documental/kernel no demuestra por sí sola que la plataforma nunca volverá a interrumpir una respuesta; la acceptance conductual debe observar el comportamiento.

## Siguiente punto verificable

1. Alinear Protocol + Adaptive Reasoning al patrón estándar ya probado en ZeroIQ, sin contaminar identidad ABOS ni añadir capas.
2. Actualizar/ejecutar acceptance conductual del kernel y verificar ProjectOps Integrity exacto.
3. Reanudar el boundary clamp P-019 desde el mismo punto, gatear HEAD exacto y continuar P-019.
