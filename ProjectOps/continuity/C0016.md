# ProjectOps — continuity/C0016.md — ABOS

State: ACTIVE
Segment: C0016
Opened: 2026-09-23
Host-Branch: `abos/p020-cognitive-fabric`

## Intervención — P-020 — Cognitive Fabric: memory, context, knowledge y activation

State: EN_EJECUCIÓN
Classification: PARTIAL / ADVERSARIAL_DEFECT_FOUND
Decision-State: DECISION_READY
Execution-Kernel: `AGENTS.md`
Plan: `P-020`
Active-Branch: `abos/p020-cognitive-fabric`

## Baseline integrado

- P-019 PR #53.
- P-019 squash merge: `3e8c0eb31652175b0f3b22ff0296d52c16e758f1`.
- exact-main CI `35918621864`: SUCCESS 8/8.
- exact-main ProjectOps `35918621868`: SUCCESS.
- P-019: HECHO / E3_MAIN_GREEN / INTEGRATION_VERIFIED.
- P-020 branch creada exactamente desde ese `main` verde.
- Audit-open commit P-020: `0da2f711bd7d0bc3875c964718019e49a7daf20d`.

## Objetivo de intervención

Reconciliar las piezas cognitivas ya existentes para que una sola authority lógica gobierne persistent knowledge/memory, activación de contexto y working set del runtime principal, sin crear “otra memoria”, sin prompt masivo y sin cerrar taxonomías futuras.

## Required-Context material

- `AGENTS.md`;
- `ProjectOps/PROJECT.md`;
- `ProjectOps/PLAN.md` + `ProjectOps/plan/P-020.md`;
- `src/agent/loop.ts`;
- `src/agent/context.ts`;
- `src/memory/`;
- `src/intelligence/`;
- `src/state/`;
- `src/skills/`;
- tests de memory/context/knowledge y consumers que la evidencia descubra.

Drift encontrado: el módulo histórico P-020 refería `src/agent/context/`, ruta inexistente. La source real consumida por `AgentLoop` es `src/agent/context.ts`; el plan fue corregido durante la activación.

## Findings verificables

### F1 — runtime principal usa stack cognitivo básico

`src/agent/loop.ts`:
- usa `MemoryRetriever` por turno;
- recupera los cinco tiers durables;
- formatea con `formatMemoryBlock()`;
- construye el prompt con `buildContextMessages()`;
- `ContextManager` y `EnhancedRetriever` no participan en el main turn path.

Existencia/test de módulos avanzados no equivale a runtime authority activa.

### F2 — `MemoryRetriever` NO es un duplicado prescindible

`src/memory/retrieval.ts` agrega, con budget, las authorities durables ya existentes:
- `working_memory`;
- `episodic_memory`;
- `semantic_memory`;
- `procedural_memory`;
- `relationship_memory`.

`EnhancedRetriever.retrieveScored()` no llama a `super.retrieve()` y sólo obtiene candidates desde `knowledge_store`. Reemplazar `MemoryRetriever` por `EnhancedRetriever.retrieveScored()` perdería los cinco tiers; H3 queda falsada como estrategia de reemplazo total.

### F3 — `ContextManager` ya preserva bundles de tool protocol

`src/memory/context-manager.ts` ya:
- tokeniza con BPE/fallback y cache acotado;
- recibe `modelContextWindow`;
- preserva system/todo/recent turns;
- renderiza cada `AgentTurn` como input → assistant/tool_calls → tool results;
- trunca tool results;
- aplica budget a task/memory/older turns/events;
- expone utilization y compactación.

Esto permite reutilizarlo como assembler del main path sin separar assistant tool_calls de sus resultados. El wiring deberá conservar además dos conductas del camino legacy que el manager aún no modela: pending input final y warning anti-repetition.

### F4 — `KnowledgeStore` es closed-world sólo en app/types

`src/memory/knowledge-store.ts` define `KnowledgeCategory` como cinco literals y `toKnowledgeEntry()` rechaza cualquier otro label.

`knowledge_store.category` en SQLite v10 es `TEXT NOT NULL`, sin CHECK enum. No se requiere migration para abrir categorías: la restricción es de aplicación.

### F5 — inferred category debe ser hint, no frontera de búsqueda

`EnhancedRetriever` infiere categorías conocidas y, cuando existen, las usa como filtro duro de `KnowledgeStore.search()`. Un label nuevo válido puede persistirse y quedar invisible aun cuando key/content coincidan con el query. La corrección debe buscar open-world y usar las categorías conocidas sólo como señal de ranking.

### F6 — feedback global es estado cross-agent accidental

`enhanced-retriever.ts` mantiene en módulo global:
- rolling precision window;
- feedback by turn;
- last rolling precision;
- una única `feedbackDb` reemplazada por cada constructor.

Dos loops/DBs en el mismo proceso pueden contaminar métricas y auto-match. Esa state no es durable cognitive authority; debe quedar instance-scoped. Reiniciar puede resetear la métrica sin perder conocimiento porque los stores durables son SQLite.

### F7 — persistence existente cubre la primera unidad sin crear otro store

- v5 ya posee working/episodic/semantic/procedural/relationship memory;
- v10 ya posee `knowledge_store` con category abierta a nivel durable, source, confidence, last_verified y expires_at;
- v18 Evidence Fabric ya posee correlation/causation, open `domain`/`authority_type`, `authority_id`, epistemic status y provenance JSON;
- P-013 conserva ownership de cross-domain causal/provenance evidence;
- `relationship_memory` conserva ownership de relaciones con entidades;
- P-022 posee la futura authority de World Model/beliefs.

Crear ahora un segundo graph/store de relaciones, facts o evidence duplicaría authorities existentes. No se abre schema v20 para la primera integración P-020. Si una prueba posterior demuestra una semántica durable imposible de representar sin falsedad, se reabre esa decisión con evidencia concreta.

### F8 — `event_stream` no se promueve automáticamente a context authority

`ContextManager` puede consumir events, pero los producers actuales no demuestran aún un único actor/identity contract para todo `event_stream` (por ejemplo, enhanced ingestion usa `sessionId` como `agentAddress`). P-020 no va a inyectar ese stream entero por mera disponibilidad. Evidence Fabric tampoco se convierte en prompt histórico masivo.

### F9 — primera unidad source aplicada y focalmente verde, pero no aceptada todavía

Source commit: `a2271119818294daa6dde25b1226f2c1879f748c`.

Cambios materiales:
- `KnowledgeStore` admite categorías open-world y stats dinámicos sin migration;
- `EnhancedRetriever` busca en todo `knowledge_store`, usa category como ranking hint y mantiene feedback por instancia;
- `ContextManager` reserva tail obligatorio y budgetea history/memory;
- `AgentLoop` usa los cinco memory tiers + activated knowledge + `ContextManager` en el camino principal;
- pending input queda al final y anti-repetition usa una helper canónica;
- el context ceiling se mantiene conservador al menor entre metadata del modelo elegido y el ceiling legacy mientras el router no exponga una ventana aggregate/fallback-safe.

Validation temporal run `35923858859` / job `107394107068`:
- knowledge-store: 22/22 PASS;
- enhanced-retriever: 23/23 PASS;
- context-manager: 28/28 PASS;
- total focal: 73/73 PASS;
- `pnpm run typecheck`: PASS;
- `pnpm run build`: PASS.

Esto es EVIDENCIA FOCAL, no cierre P-020.

### F10 — adversarial review encontró tool-call-only protocol gap

Tras el focal green se releyó el source aplicado. `ContextManager.renderTurn()` sólo crea el mensaje `assistant` cuando `turn.thinking` tiene texto. Sin embargo un turno con tool calls y assistant content vacío es válido y `AgentLoop` persiste precisamente `thinking: response.message.content || ""`.

Consecuencia: para un tool-call-only turn, el contexto reconstruido puede emitir mensajes `tool` sin el mensaje `assistant` precedente que declara sus `tool_calls`, rompiendo el protocolo de conversación que P-020 debe preservar.

El test adversarial existente usaba `thinking: "calling tool"`, por lo que no falsaba este caso.

Clasificación: DEFECTO REAL / RECUPERABLE. No invalida H1 ni exige arquitectura nueva. La decisión correcta es **CORRECT** `ContextManager.renderTurn()` y añadir regresión explícita para tool-call-only turns antes de aceptar el wiring.

### F11 — full-suite encontró una incompatibilidad de test/API legacy, no un defecto del scorer nuevo

Full CI exacto de branch sobre `fb170c0497d9767421d2d59f83f529c20f998523`, run `35925163322`, llegó a typecheck PASS + build PASS y ejecutó 2120 tests. Resultado: 2119 PASS / 1 FAIL.

Único fallo: `src/__tests__/integration/memory-retrieval.test.ts` todavía importa y llama la función de módulo `recordRetrievalFeedback()` en un caso, mientras el resto del test y el runtime ya usan `EnhancedRetriever.recordRetrievalFeedback()` por instancia.

La función global fue retirada deliberadamente para eliminar `feedbackDb`/rolling precision compartidos entre DBs y agentes. Restaurarla con estado global reintroduciría F6 y violaría la decisión ya alcanzada. La corrección compatible con la authority seleccionada es actualizar ese caller legacy para usar la instancia `retriever`, no reintroducir una segunda/global feedback authority.

Clasificación: **CORRECT TEST/CALLER**. No cambia arquitectura ni plan. Windows 22/24, typecheck, build, ProjectOps y las suites cognitivas focales permanecen verdes en el mismo HEAD; el gate de aceptación sigue abierto hasta full CI verde.

## Hipótesis — resultado de discriminación

- H0 `NO_CHANGE`: FALSADA por F1/F4/F5/F6.
- H1 `EXTEND_AND_WIRE_EXISTING_MEMORY_CONTEXT_FABRIC`: CONFIRMADA; estrategia seleccionada.
- H2 `CREATE_NEW_COGNITIVE_MANAGER`: RECHAZADA; duplicaría componentes reales.
- H3 `REPLACE_MEMORY_RETRIEVER_COMPLETELY`: FALSADA por F2.
- H4 `NEW_DB_REQUIRED_FOR_OPEN_CATEGORIES`: FALSADA; v10 ya es open TEXT.
- H5 `NEW_DB_REQUIRED_FOR_RELATIONS/TEMPORAL/PROVENANCE`: FALSADA para la primera unidad P-020; v5/v10/v18 y ownership P-022 cubren la semántica material conocida sin segunda authority. Se reabre sólo ante evidencia discriminante nueva.

## Decisión técnica — DECISION_READY

Classification por pieza:

1. `MemoryRetriever`: **REUSE** como primitive de los cinco memory tiers.
2. `KnowledgeStore`: **CORRECT/EXTEND** para categorías open-world y stats dinámicos, sin migration.
3. `EnhancedRetriever`: **CORRECT/EXTEND** como activador/scorer suplementario de `knowledge_store`; category inference se vuelve hint, no filtro; feedback queda instance-scoped.
4. `ContextManager`: **REUSE/EXTEND** como assembler model-aware del main turn path; debe preservar pending input y anti-repetition sin romper tool protocol.
5. `buildContextMessages`: **REFACTOR/COMPATIBILITY**; conserva API legacy/tests donde existan, pero el main runtime deja de depender de él como authority de assembly una vez que `ContextManager` cubra su contrato material.
6. Persistence/schema: **NO_CHANGE** en esta unidad.
7. Evidence/relations/intelligence/skills: **REUSE**, sin absorber sus ownerships dentro de P-020.

Las incertidumbres restantes ya no cambian materialmente esta primera decisión ni exigen otra authority; el gate `DECISION_READY` queda satisfecho.

## Invariantes de implementación

1. Ningún wiring puede perder working/episodic/semantic/procedural/relationship memory.
2. Un category hint jamás puede ocultar un match open-world.
3. Tool call assistant message y tool result deben permanecer juntos y ordenados, incluso cuando assistant content sea vacío.
4. Pending input debe seguir siendo el último input del turno.
5. Anti-repetition debe conservarse como protección del loop.
6. Model context window se obtiene de ModelRegistry cuando exista; ausencia degrada a budget legacy, no a valor inventado.
7. Context/feedback in-memory puede reiniciarse; cognitive facts necesarios para recovery deben venir de SQLite.
8. No event/evidence dump masivo en prompt.
9. No nueva schema/migration salvo nueva evidencia discriminante.

## Plan inmediato de ejecución

1. Corregir el único caller legacy de feedback para usar la instancia `EnhancedRetriever`, sin restaurar estado global.
2. Reejecutar full CI/ProjectOps exactos; mantener focal cognitive + typecheck + build como gates explícitos.
3. Completar validaciones P-020 todavía no demostradas: stale facts, restart/recovery, activation/deactivation y conflicting memories/evidence según authorities reales.
4. Auditar producers de `knowledge_store` para comprobar que activation tiene una ruta material de población y no sólo un consumer conectado.
5. Eliminar tooling temporal residual antes de PR.
6. Reconciliar continuity/plan según evidencia; no cerrar P-020 sólo por source escrito ni por focal green.
