# ProjectOps — continuity/C0019.md — ABOS

State: ACTIVE
Segment: C0019
Opened: 2026-09-24
Host-Branch: `abos/p023-prediction-learning`

## Intervención — P-023 — Prediction → Outcome → Error → Learning

State: EN_EJECUCIÓN
Classification: DECISION_READY / SOURCE_IMPLEMENTED / EXACT_HEAD_CI_FAIL
Decision-State: DECISION_READY
Decision-Class: REUSE_ADAPTIVE_PATH_ATTEMPT + EXTEND_CAUSAL_COMPARISON + CORRECT_UNKNOWN_ATTRIBUTION
Execution-Kernel: `AGENTS.md`
Plan: `P-023`
Baseline-Main: `ade7d29148f6c49df94464c52dc22b8b2e2cb77f`
Baseline-Exact-Main-CI: `36048851506` — SUCCESS 8/8
Starting-Branch-Head: `ade7d29148f6c49df94464c52dc22b8b2e2cb77f`
Activation-Head: `3c75cd863389a2b8a249af8bfc7936f6cd0b4a9d`
Activation-CI: `36049624768` — SUCCESS 8/8
Last-Observed-Product-Head: `e344958200449b4fcc01ec11ae93eb0ce2f152fc`
Last-Observed-Main: `ee033e5586dbf7bcbb2dbfac88ad12e529a38972`
Last-Observed-CI: `36053162495` push / `36053167900` PR — FAILURE

## Dependencias satisfechas

- P-013: HECHO / Evidence Fabric integrado.
- P-020: HECHO / Cognitive Fabric integrado.
- P-021: HECHO / Skill Evolution integrado.
- P-022: HECHO / World Model integrado por PR #62 en `main ade7d29148f6c49df94464c52dc22b8b2e2cb77f`, exact-main CI `36048851506` SUCCESS 8/8.

No existía una rama `p023` recuperable al abrir esta intervención. P-023 se activó sólo después de demostrar P-022 terminal.

## Objetivo activo

Cerrar el loop canónico definido por P-023:

`OBSERVE → MODEL → PREDICT → ACT → MEASURE → COMPARE → ATTRIBUTE → LEARN → UPDATE`.

Para acciones materiales, ABOS debe dejar un prediction record previo, observar outcome posterior, comparar sin leakage retrospectivo, atribuir el error a la causa apropiada y actualizar la authority correcta sin fabricar métricas ni convertir UNKNOWN en error de modelo.

## Auditoría de authority, producers, consumers y temporalidad

### Prediction record existente — REUSE

`adaptive_paths` ya es el prediction record durable y previo a la ejecución estratégica:

- `PathCandidate` contiene `hypothesis`, `assumptions`, `requiredCapabilities`, `environment`, `executor`, `sequence`, `expectedOutcome`, `expectedCostCents` y evidence refs;
- el path se crea/selecciona antes de `dispatchAgentTask`/mensajería;
- el planner expone `PlannerPathIntent.expectedOutcome` y la hipótesis estructurada antes de descomponer/ejecutar tareas;
- `pathSignature()` preserva identidad conceptual de estrategia, hipótesis, assumptions, environment/executor, sequence y expected outcome;
- `adaptive_paths.created_at` y los eventos causales prueban orden prediction-before-outcome.

Conclusión: crear `adaptive_predictions` duplicaría authority y fue RECHAZADO.

### Outcome record existente — REUSE

`adaptive_attempts` ya es el outcome record durable:

- cada attempt referencia `path_id` y opcional `task_id`;
- outcome soporta `success`, `partial_success`, `failed`, `blocked`, `unavailable`, `unknown`, `inconclusive`;
- guarda failure class/reason, observations, evidence, condition fingerprint, novelty, learned facts y retry eligibility;
- `recordSuccess()` y `recordFailure()` se ejecutan sólo después de un resultado/exception de ejecución;
- el Orchestrator enlaza tasks planificadas al strategic path y late results de rutas superseded no pueden resucitar el plan actual.

Conclusión: crear otro outcome ledger fue RECHAZADO.

### Evidence Fabric — REUSE REFERENCIAL

P-013 declara explícitamente que Evidence Fabric posee correlation/causation cross-domain y nunca canonical adaptive state. Es la authority correcta para registrar el evento `prediction compared / attributed`, referenciando path + attempt, no para duplicar prediction/outcome values como nueva source of truth.

### Skill Evolution — CONSUMER CONDICIONAL, NO OWNER UNIVERSAL

Skill Evolution ya tiene `skill_evaluations`, outcomes `success/failure/partial/inconclusive`, evidence refs, task/environment IDs, promotion/degradation lifecycle e independencia de evidence. Sin un `skillVersionId` causal explícito en un attempt concreto no se permite inferir que el error pertenece a una skill. P-023 no fabricará esa relación.

### Learning existente

- success terminal valida assumptions y marca path succeeded;
- `assumption_invalid` invalida el assumption correspondiente o degrada a UNKNOWN cuando no se puede discriminar cuál;
- terminal path failure invalida el strategic world belief con failure evidence;
- failed/equivalent path + unchanged conditions no puede repetirse como si no existiera evidencia nueva;
- capability/environment/resource/authorization failures generan rutas de acquisition/change/wait sin declararlas imposibles.

Esto ya implementa gran parte de `LEARN → UPDATE`; falta hacer explícita y causal la comparación/atribución sin duplicar state.

## Defecto material encontrado

`FailureClass` incluye `unknown`, pero `classifyFailure()` no poseía ninguna salida `unknown`: cualquier error no reconocido caía al default `strategic_failure`, que era terminal.

Efectos observables previos:

1. evidencia no clasificable se convertía en error estratégico/modelo;
2. `recordFailure()` persistía outcome `failed`;
3. el path terminaba `failed`;
4. el strategic belief podía invalidarse aunque la evidencia no demostrara contradicción.

Eso violaba P-023 y el invariante transversal `UNKNOWN is first-class`.

## Hipótesis discriminadas

- H0 `NO_CHANGE`: **FALSADA COMO LOOP COMPLETO**. Prediction/outcome/learning parcial ya existen, pero comparison/attribution explícito faltaba y UNKNOWN podía convertirse en strategic failure terminal.
- H1 `EXTEND_ADAPTIVE_INTELLIGENCE`: **CONFIRMADA EN ALCANCE MÍNIMO**. Paths/attempts son las canonical authorities para prediction/outcome; no se añade un segundo ledger.
- H2 `EXTEND_EVIDENCE_FABRIC`: **FALSADA COMO OWNER / CONFIRMADA COMO FABRIC REFERENCIAL**. Sólo registra causal comparison/attribution events.
- H3 `EXTEND_SKILL_EVOLUTION`: **FALSADA COMO OWNER UNIVERSAL / CONFIRMADA COMO CONSUMER CONDICIONAL** cuando exista skill-version causal explícita.
- H4 `COMPOSE_EXISTING_AUTHORITIES`: **CONFIRMADA**.
- H5 `CREATE_PARALLEL_PREDICTION_MANAGER`: **RECHAZADA**.

## Decisión canónica

`DECISION_READY`:

1. REUSE `adaptive_paths` como prediction record previo.
2. REUSE `adaptive_attempts` como outcome record posterior.
3. EXTEND con una función de comparison/attribution pura y un Evidence Fabric event causal, sin nueva state authority.
4. CORRECT failure classification para que evidencia no reconocida resulte `unknown`, no `strategic_failure`.
5. Mantener patrones explícitos de strategic/model failure para evidencia que sí demuestra que la estrategia produjo un resultado inválido.
6. Mapear UNKNOWN a outcome `inconclusive`/path `unknown`, sin invalidar strategic belief.
7. Registrar comparison:
   - path final success → `confirmed`;
   - terminal strategic/assumption/prohibited/impossible contradiction → `contradicted`;
   - transient/capability/environment/resource/authorization/unknown → `inconclusive` con attribution específica, sin castigar el modelo.
8. Learning targets son referenciales y corresponden a authority real: assumption/world-belief/path/capability/environment/authorization/execution evidence. No se actualiza Skill Evolution sin skill-version causal explícita.
9. No calcular Brier/calibration mientras las predictions actuales tengan confidence no calibrada/null o muestra semánticamente insuficiente. `NO METRIC` es el resultado correcto, no un número inventado.

## Implementación observable

El delta decidido ya existe en la rama activa:

- `3e6bedffbbe699e2b57b0dfa958fa7354016e233` corrige UNKNOWN para que no sea terminal por default;
- `33d293ab101ca1da185db2dd33d8cf0a3856001c` conecta comparison/attribution al runtime adaptativo;
- `1a86db9d87190a0b047089b8ed0b5b32815e635a` añade validación P-023;
- `e344958200449b4fcc01ec11ae93eb0ce2f152fc` retira el workflow P-010 obsoleto de la rama.

Archivos product relevantes:
- `src/intelligence/failure-classifier.ts`;
- `src/intelligence/prediction-learning.ts`;
- `src/intelligence/adaptive-engine.ts`;
- `src/__tests__/p023-prediction-learning.test.ts`.

La branch está 13 commits ahead y 1 behind de `main ee033e5586dbf7bcbb2dbfac88ad12e529a38972`; el único commit de main posterior al baseline retira el mismo workflow P-010 mediante otro SHA. Reconciliar historia antes de recuperar el gate no aporta evidencia sobre el fallo actual y se difiere hasta después del fix.

## Gate actual y fallo discriminado

CI exact-head sobre `e344958200449b4fcc01ec11ae93eb0ce2f152fc`:

- push `36053162495`: FAILURE;
- PR #63 `36053167900`: FAILURE;
- ProjectOps integrity: PASS;
- typecheck: PASS Node 22/24;
- build: PASS Node 22/24;
- Windows regression: PASS Node 22/24;
- public distribution smoke: PASS Node 22/24;
- security audit: PASS;
- P-023 focused tests: PASS 7/7;
- full suite: FAIL exactamente 1 test; 2154 PASS, 1 FAIL, 1 SKIP en Node 22, con el mismo job class fallando en Node 24.

Fallo exacto:

`src/__tests__/p022-strategic-belief-runtime.test.ts` espera que `Execution produced an unrecoverable strategic mismatch.` sea evidencia terminal. El nuevo clasificador la deja `unknown` porque P-023 eliminó el antiguo default `strategic_failure` pero todavía no añadió matching explícito para esa evidencia estratégica.

Discriminación:

- NO es motivo para restaurar `strategic_failure` como default: eso reintroduciría el defecto P-023.
- NO es correcto cambiar el test para aceptar UNKNOWN: el texto declara explícitamente un mismatch estratégico unrecoverable y el test P-022 protege invalidación terminal ante evidencia estratégica.
- CORRECT: extender únicamente el matching explícito de `strategic_failure` para cubrir strategic mismatch/contradiction semánticamente explícitos; mantener todo input no discriminante como UNKNOWN.

## Invariantes

1. Prediction precede outcome; no retrospective leakage.
2. Un execution failure no equivale a model error.
3. Measurement/UNKNOWN no se convierte en outcome negativo inventado.
4. Learning actualiza sólo la authority causalmente responsable.
5. No duplicar canonical beliefs, skills, evidence, paths, tasks ni outcomes.
6. Restart preserva path + attempt + causal Evidence events.
7. Reprocessing no puede aplicar learning terminal dos veces; existing belief invalidation y path status transitions deben seguir idempotentes.
8. Calibration sólo con probability semantics y muestra suficiente.
9. P-024 simulation y P-025 strategic review no se adelantan.

## Plan inmediato autorizado

1. Añadir matching explícito y acotado para `strategic mismatch/contradiction` en `classifyFailure()` sin tocar el default UNKNOWN.
2. Añadir/ajustar test P-023 adversarial que demuestre simultáneamente que un strategic mismatch explícito sigue siendo terminal y un mensaje opaco permanece UNKNOWN.
3. Reejecutar exact-head CI completo; no aceptar sólo el test focal.
4. Auditar luego los invariantes P-023 todavía no demostrados por los 7 tests actuales: assumption attribution, external-condition attribution, restart/pairing e idempotencia/resolution.
5. Revisar que Evidence Fabric sea referencial y no se convierta en segunda authority de prediction/outcome.
6. Reconciliar la rama con `main ee033e5586dbf7bcbb2dbfac88ad12e529a38972` sólo cuando el product gate esté recuperado, revalidando exact-head después de reconciliar.
7. Integrar PR #63 únicamente tras branch+PR gates verdes y exigir exact-main CI antes de cierre terminal.

## Evidencia disponible

- baseline exact-main `ade7d29148f6c49df94464c52dc22b8b2e2cb77f`, CI `36048851506` SUCCESS 8/8;
- activation/reconciliation head `3c75cd863389a2b8a249af8bfc7936f6cd0b4a9d`, CI `36049624768` SUCCESS 8/8;
- current observed source head `e344958200449b4fcc01ec11ae93eb0ce2f152fc`;
- current exact-head CI push `36053162495` / PR `36053167900`: FAILURE por una regresión P-022 discriminada arriba;
- product source P-023 modificado: SÍ;
- P-023 tests actuales: PASS 7/7 dentro del full suite fallido.

## Siguiente punto verificable

Corregir exclusivamente el gap explícito de clasificación estratégica, demostrar que UNKNOWN sigue preservado, y recuperar el full exact-head gate antes de cualquier claim SOURCE_COMPLETE o integración.