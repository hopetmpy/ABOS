# ProjectOps — continuity/C0025.md — ABOS

State: CLOSED
Segment: C0025
Opened: 2026-09-25
Closed: 2026-09-26
Host-Branch: `abos/p029-child-bootstrap-family-knowledge`

## Intervención — P-029 — child bootstrap, Constitution gate y Family Knowledge Fabric

State: HECHO
Classification: SOURCE_COMPLETE / E3_MAIN_GREEN / INTEGRATION_VERIFIED
Decision-State: DECISION_READY
Execution-Kernel: `AGENTS.md`
Plan: `P-029`
Baseline-Main: `803802f68cc3c1b7c1bd8d850768ca924a18aca2`
Baseline-Main-CI: `36206237455` — SUCCESS 8/8
Source-Complete-Head: `2fe9fda35d99cde8759bccfc1b836a2e749106bb`
Source-Complete-CI: `36220144160` — SUCCESS 8/8
Final-PR-Head: `24924c4bc04f7b77571d0c10f53c3de7a631f06a`
Final-PR-Head-CI: `36220441096` — SUCCESS 8/8
PR: `#74` — MERGED
Merge: `d14f04b9fdea98eecd9d8e772b223b55d1fcf010`
Exact-Main-CI: `36220600132` / run #2044 — SUCCESS 8/8

## Dependencias demostradas

P-009, P-010, P-011, P-014, P-020, P-021 y P-028 estaban HECHO/integradas con evidencia exact-main antes de cerrar P-029. P-028 cerró por PR #71, merge `803802f68cc3c1b7c1bd8d850768ca924a18aca2`, exact-main CI `36206237455` SUCCESS 8/8.

Durante P-029 `main` avanzó por reconciliación del execution kernel; la branch fue reconciliada y llegó al merge 0 commits behind respecto del main auditado antes de PR #74.

## Objetivo alcanzado

El birth/bootstrap de children ya no puede promover un child a operativo sólo porque exista un proceso. Runtime/identity/wallet/constitution/context familiar mínimo quedan causalmente verificados antes de `healthy`, y Family Knowledge usa herencia/consulta selectiva con provenance sin copiar indiscriminadamente memoria ni crear authorities paralelas.

## Hipótesis resueltas

- H0 `NO_CHANGE`: **FALSADA**. Había primitives pero wiring causal incompleto.
- H1 `CORRECT_EXISTING_CHILD_BOOTSTRAP`: **CONFIRMADA Y EJECUTADA**.
- H2 `EXTEND_EXISTING_MEMORY_KNOWLEDGE`: **CONFIRMADA Y EJECUTADA**.
- H3 `UNIFY_EXISTING_PROPAGATION_PATHS`: **CONFIRMADA Y EJECUTADA**.
- H4 `CREATE_NARROW_FAMILY_KNOWLEDGE_PROJECTION`: **CONFIRMADA COMO ADAPTER/PROJECTION**, no como store/authority independiente.

## Decisión implementada

- Constitution helpers: `REUSE + CORRECT WIRING`.
- spawn/legacy spawn: `CORRECT`, propagation fail-closed y verificación causal.
- runtime start/health: `CORRECT`, liveness no equivale a bootstrap/healthy.
- genesis bootstrap/KnowledgeStore: `EXTEND` con bundle familiar versionado, receipt/hash e import idempotente.
- Skills/Capabilities: `REUSE AS CATALOG SOURCES`, sin heredar execution authority.
- Family Knowledge: `CREATE_NARROW ADAPTER`, no segunda memory authority.
- query selectiva: `EXTEND EXISTING COLONY MESSAGING` sobre `peer_query`/`peer_response`, Social relay, inbox durable y KV existentes.
- schema: `NO_CHANGE`; las autoridades existentes bastan.

## Cambios materiales integrados

- `propagateConstitution()` dejó de ser best-effort en birth paths y el gate verifica constitution material.
- Family Knowledge se materializa antes de `--init`, se importa idempotentemente y deja provenance ligada a parent/child.
- bootstrap attestation usa KV canónico, queda ligada a child/sandbox/wallet/constitution/family hash y se elimina al fallar una re-verificación.
- `ensureChildRuntimeRunning()` y `ChildHealthMonitor` requieren bootstrap válido además de process liveness.
- fallo transitorio de bootstrap no terminaliza `funded/starting/unhealthy`; un `healthy` inválido se degrada a `unhealthy` recuperable.
- reused sandbox limpia estado bootstrap stale antes de reasignación.
- skills/capabilities parent se heredan como catálogo/conocimiento, no como capability ejecutable del child.
- query Family Knowledge usa pending/result durable en KV, autorización de child directo, validación de parent canónico, recovery tras restart e idempotencia de replay.
- `query_family_knowledge` y `family_knowledge_query_status` se exponen por el facade canónico de tools.
- heartbeat conserva `check_social_inbox` como único poller; P-029 sólo despacha carriers desde el inbox durable existente.

## Adversarial review y regresiones

Demostrado:
- constitution missing/tampered bloquea bootstrap;
- config/wallet/parent mismatch bloquea bootstrap;
- proceso running sin bootstrap no puede producir `healthy`;
- bootstrap failure transitorio conserva recovery en vez de fabricar terminalidad;
- bundle/import y replay son idempotentes;
- skill/capability inheritance no concede authority ejecutable;
- requester debe ser child directo conocido/no terminal;
- wrong-parent y unsolicited response se rechazan;
- pending query sobrevive restart;
- child conserva knowledge importado offline;
- payload peer no-Family se ignora por el adapter;
- fixtures históricas P-011/lifecycle fueron corregidas para aportar bootstrap real, sin bypasses de producción.

SOFT residual conocido:
- el responder canónico respeta `request.limit` y el parser limita globalmente a 20 items, pero el apply path no vuelve a comparar `response.knowledge.length` con el `pending.limit`. No hay evidencia de bypass por el responder autorizado; queda como hardening futuro si cambia el threat model.

## Validación

### Branch source-complete

Head `2fe9fda35d99cde8759bccfc1b836a2e749106bb` — CI `36220144160` SUCCESS 8/8.

### Final PR checkpoint

Head `24924c4bc04f7b77571d0c10f53c3de7a631f06a` — CI `36220441096` SUCCESS 8/8.

### Integración exact-main

PR #74 fue mergeado como `d14f04b9fdea98eecd9d8e772b223b55d1fcf010`.
Exact-main CI `36220600132` / #2044: **SUCCESS 8/8**.

La matriz verde incluye:
- `build-and-test (22)`;
- `build-and-test (24)`;
- `windows-regression (22)`;
- `windows-regression (24)`;
- `public-distribution-smoke (22)`;
- `public-distribution-smoke (24)`;
- `security-audit`;
- `rebrand-integrity`.

Los build-and-test ejecutaron ProjectOps integrity, typecheck, build, suite completa y security tests.

## Evidence level y límites

Evidence level alcanzado: **E3 / exact-main integration verified**.

NO demostrado:
- birth físico/LIVE contra Conway real;
- cuentas, permisos, credits/gasto real, KYC/firma externa o disponibilidad de infraestructura Conway;
- E4/E5/E6.

Esto no invalida el cierre source/integration de P-029; las fronteras LIVE permanecen bajo P-005/autoridades externas cuando existan entorno y autorización reales.

## Estado del plan

La intención/arquitectura/DoD de `plan/P-029.md` no cambió. El manifest `PLAN.md` debe reflejar P-029 `HECHO` por PR #74 + main `d14f04b9...` + exact-main CI `36220600132`.

## Handoff

P-029 queda cerrado y recuperable con evidencia exacta. La siguiente unidad elegible por dependencias es P-030 — Family Economics y Treasury causal.

La continuidad viva pasa a `continuity/C0026.md`; no se abre source P-030 hasta completar su auditoría de autoridades, owners, flows financieros, persistencia, idempotencia, reconciliation y alcanzar `DECISION_READY`.