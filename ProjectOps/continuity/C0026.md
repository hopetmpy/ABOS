# ProjectOps — continuity/C0026.md — ABOS

State: ACTIVE
Segment: C0026
Opened: 2026-09-26
Host-Branch: `abos/p030-family-economics-treasury`

## Intervención — P-030 — Family Economics y Treasury causal

State: EN_EJECUCIÓN
Classification: DECISION_READY / IMPLEMENTATION_PENDING
Decision-State: DECISION_READY
Execution-Kernel: `AGENTS.md`
Plan: `P-030`
Baseline-Main: `d14f04b9fdea98eecd9d8e772b223b55d1fcf010`
Baseline-Main-CI: `36220600132` / #2044 — SUCCESS 8/8
Starting-Branch-Head: `d14f04b9fdea98eecd9d8e772b223b55d1fcf010`
Audit-Checkpoint-Before-Source: `dac8d555d0d97cd023c4692c8fc1731de26bf3a5`
Product-Source-At-Decision-Gate: UNCHANGED

## Dependencias demostradas

- P-003: HECHO/integrado; posee la semántica canónica funding != balance != expense y la separación internal capital vs external P&L.
- P-009: HECHO/integrado.
- P-010: HECHO/integrado; Policy execution posee lifecycle/evidence, pero no idempotencia económica cross-decision.
- P-013: HECHO/integrado; Evidence Fabric correlaciona transactions/spend/policy sin poseer dinero.
- P-023: HECHO/integrado; `adaptive_paths` + `adaptive_attempts` son prediction/outcome authorities y se REUTILIZAN para learning económico.
- P-025: HECHO/integrado; Strategic Cognition aporta juicio causal, alternatives y `REAL_BOUNDARY | GOVERNABLE_RISK`.
- P-027: HECHO/integrado por PR #70; opportunity hypotheses ya poseen expected cost/upside, uncertainty, falsification y evidence refs.
- P-029: HECHO/integrado por PR #74; merge `d14f04b9fdea98eecd9d8e772b223b55d1fcf010`; exact-main CI `36220600132` SUCCESS 8/8.

## Objetivo

Extender la semántica de capital ya corregida por P-003 hacia una treasury familiar causal que distinga dinero observado, libre, comprometido, reservado, asignado, gastado/realizado, retornado y revenue/P&L externo, con idempotencia, reconciliation, provenance y decisiones adaptativas sin thresholds rígidos convertidos en verdad universal.

## Invariantes

- funding != balance != expense;
- allocation/return internos != revenue/P&L externo;
- estimate != commitment != realized cost;
- expected revenue != realized revenue;
- UNKNOWN profitability != loss;
- available balance != free capital;
- parent accounting != child/provider/wallet observation;
- refund pending != liquidez disponible;
- no crear segunda treasury/ledger/persistence/policy/evidence authority por conveniencia;
- financial side effects exigen authority, idempotency y reconciliation antes de success;
- creator approval sólo ante frontera real/oversight explícito, no como política económica por defecto.

## Auditoría causal completada hasta DECISION_READY

### Authorities existentes que se REUTILIZAN

- `transactions`: ledger canónico de movimientos financieros materializados/realizados; ya distingue `capital_allocation` y `capital_return`.
- `children.funded_amount_cents`: bookkeeping histórico parent→child; no es balance observado.
- `task_graph`: owner de estimate/actual task cost y assignments; sirve para commitments operativos sin duplicarlos.
- `spend_tracking`: owner de ventanas de policy/risk accounting; no es business P&L ni ledger de cash.
- `policy_decisions`: authority de autorización/ejecución protegida y estado `running/succeeded/failed/unknown`; no deduplica efectos económicos entre decisiones nuevas.
- Evidence Fabric: correlation/provenance, nunca segunda contabilidad.
- `adaptive_paths` + `adaptive_attempts`: prediction/outcome/learning; no crear decision-memory paralela.
- `adaptive_opportunities`: portfolio/hypotheses existentes con expected cost/upside/uncertainty.
- `src/economics/child-economics.ts`: proyección read-only válida para child economics; preserva UNKNOWN y ROI sólo con numerator/denominator válidos.

### HARD findings demostrados

1. `transfer_credits` y `fund_child` contienen un guard hard-coded `amount > balance / 2`.
   - contradice P-030: un porcentaje fijo no puede ser verdad universal de racionalidad;
   - también duplica/precede al policy layer y puede bloquear una decisión extraordinaria causalmente justificada.

2. `src/survival/funding.ts` hace `getCreditsBalance().catch(() => 0)`.
   - colapsa UNKNOWN/error de observación a cero;
   - puede disparar comportamiento de crisis/funding desde evidencia inexistente.

3. `isCreditTransferAccepted()` trata como aceptado cualquier status que no contenga un marcador negativo.
   - `submitted`, `processing`, `pending` o un status futuro no reconocido pueden convertirse en transaction/capital allocation realizada;
   - P-030 exige que `transfer submitted unknown` permanezca pendiente/UNKNOWN hasta reconciliation.

4. `ConwayClient.transferCredits()` genera un idempotency key ULID nuevo en cada llamada.
   - el key se reutiliza sólo entre los dos endpoint fallbacks de ESA llamada;
   - un retry lógico posterior tras respuesta perdida usa otro key y puede double-execute.

5. `SimpleFundingProtocol.fundChild()` atrapa cualquier excepción de transfer y devuelve `{ success:false }`.
   - una respuesta perdida después de dispatch queda indistinguible de un fallo pre-effect;
   - Orchestrator convierte `false` en failure/replan y puede repetir funding.

6. El mismo `SimpleFundingProtocol` toma la decisión inversa ante external success + local SQLite failure: loggea el reconciliation gap y devuelve `{ success:true }`.
   - existen dos semánticas incompatibles para incertidumbre del mismo efecto;
   - no hay owner durable que permita reconciliar después de restart.

7. `policy_decisions.scope_hash` y `execution_state` no son idempotencia económica.
   - cada evaluación nueva genera otro decision id;
   - no existe una clave durable de logical financial effect que sobreviva una nueva decisión/restart.

### SOFT findings / límites de evidencia

- `transactions` no debe convertirse en intent log: carece de status/recipient/provider-id/idempotency y representa movimientos materializados.
- `spend_tracking` conserva utilidad como guard/risk signal, pero sus caps fijos son transicionales y no deben sustituir juicio treasury.
- `src/economics/child-economics.ts` tiene atribución incompleta cuando tasks fallidas/replanned limpian `assigned_to`; no se fabricará ownership retrospectivo.
- no se ha demostrado una API provider de lookup/reconcile de credit transfer por id; P-030 debe soportar reconciliation con observed external events inyectados y dejar provider-native lookup como capability futura si aparece.
- LIVE Conway/funds reales siguen NO VERIFICADOS y pertenecen a P-005/autorización externa.

## Hipótesis discriminadas

- H0 `NO_CHANGE`: FALSADA. Los HARD findings anteriores contradicen los invariantes/DoD de P-030.
- H1 `EXTEND_EXISTING_CAPITAL_LEDGER`: CONFIRMADA PARCIAL. `transactions` y owners actuales se reutilizan para estado realizado, pero no deben almacenar intents/settlement ambiguity.
- H2 `UNIFY_DUPLICATED_ECONOMIC_RECORDS`: CONFIRMADA COMO COMPOSICIÓN, NO MIGRACIÓN. Transactions, spend, task cost, child bookkeeping y Evidence tienen authorities distintas; una treasury projection debe componerlas sin copiar/colapsar ownership.
- H3 `CREATE_NARROW_TREASURY_DOMAIN_LAYER`: CONFIRMADA. Falta una capa de dominio estrecha para projection/decision/reconciliation, sin ser segundo ledger.
- H4 `CORRECT_SIDE_EFFECT_RECONCILIATION`: CONFIRMADA HARD. Transfer/funding actuales permiten late/unknown outcome y retry con idempotency key nuevo.
- H5 `EXTEND_EXISTING_LEARNING`: CONFIRMADA COMO REUSE/COMPOSE. P-023/P-025/P-027 ya poseen prediction/outcome/opportunity judgment; P-030 sólo aporta semántica económica y evidence refs.

## Decisión arquitectónica

### 1. Ledger realizado se conserva

`transactions` sigue siendo authority de movimientos materializados/realizados. No se insertará una transaction como realizada cuando provider sólo diga `submitted/processing/pending` o el resultado de dispatch sea UNKNOWN.

### 2. Crear sólo un owner estrecho de external financial effects

Se autoriza `CREATE_NARROW` de una state machine persistente para **financial effect intent/reconciliation**, no otra treasury/ledger.

Debe poseer únicamente:
- durable logical operation/idempotency key;
- effect kind + canonical semantic fingerprint;
- amount + unidad;
- recipient/child cuando aplique;
- provider external id/status observado;
- estado pre-dispatch / pending-or-unknown / settled / rejected / reconciliation-required o equivalentes;
- timestamps y evidence/provenance mínima.

No poseerá P&L, balances derivados, opportunity state, strategic prediction/outcome ni policy authority.

### 3. Idempotencia antes del side effect

- persistir intent ANTES de dispatch;
- usar su durable key como provider idempotency key;
- un logical effect equivalente no puede volver a dispatch mientras exista un effect no terminal compatible;
- ambiguous transport/5xx -> UNKNOWN/PENDING, no retry ciego;
- explicit reject -> rejected, sin transaction;
- explicit settled observation -> materialización idempotente de transaction + child bookkeeping cuando corresponda;
- settled externally + local projection failure -> reconciliation-required, no success ficticio ni second transfer.

### 4. Clasificación rica de provider status

Reemplazar la semántica binaria `isCreditTransferAccepted()` por clasificación material:
- explicit negative -> REJECTED;
- explicit final settlement/success -> SETTLED;
- submitted/processing/pending/queued/empty/unrecognized -> PENDING_OR_UNKNOWN.

UNKNOWN nunca se convierte en SETTLED por ausencia de palabras negativas.

### 5. Unificar callers sobre el mismo execution/reconciliation adapter

El mismo adapter debe ser usado por:
- `transfer_credits`;
- `fund_child`;
- `SimpleFundingProtocol.fundChild()` / orchestrator funding.

Así desaparece la divergencia semántica actual sin crear otro transport.

### 6. Treasury projection causal

Crear/EXTEND una proyección de dominio que componga owners existentes y calcule, sólo cuando la evidencia lo permita:
- observed parent credits/cash por autoridad y unidad;
- active commitments desde task graph;
- pending/unknown financial effects como capital inmovilizado, no realizado;
- explicit reserves/contingency inputs;
- internal capital allocated/returned;
- realized known operating costs;
- child economics existentes;
- free capital sólo si balance observado y deductions requeridas son conocidas.

Si balance o una deducción material necesaria es UNKNOWN, free capital permanece UNKNOWN; no se sustituye con cero.

### 7. Thresholds

- eliminar el universal `50% balance` de tool paths;
- caps configurables existentes pueden persistir inicialmente como guard/risk signal transicional, pero no se presentarán como verdad económica universal;
- no introducir otro porcentaje fijo de reserva/reinvestment/distribution.

### 8. Learning y judgment

- REUSE P-023 prediction→outcome;
- REUSE P-025 Strategic Cognition para ordinary/extraordinary decisions;
- REUSE P-027 opportunity portfolio;
- Treasury emitirá/consumirá referencias económicas causales, no tablas paralelas de predictions/outcomes.

## Decision gate

`DECISION_READY` alcanzado porque las incertidumbres restantes no cambian materialmente la decisión:
- existe un gap probado de idempotency/reconciliation;
- no existe owner semánticamente equivalente para financial-effect intent;
- los ledgers/policy/evidence existentes tienen ownership distinto y se preservan;
- provider-native reconciliation lookup sigue NO DESCUBIERTO/NO DISPONIBLE en el contrato actual, pero reconciliation inyectada permite implementar y validar la state machine sin inventar provider capability.

## Secuencia inmediata autorizada

1. materializar narrow financial-effect state + migration mínima;
2. extender Conway transfer para aceptar durable idempotency key;
3. crear classifier + execution/reconciliation adapter central;
4. migrar `transfer_credits`, `fund_child` y `SimpleFundingProtocol` al adapter;
5. eliminar guard universal 50%;
6. corregir UNKNOWN→0 en funding/balance observations donde esté demostrado;
7. crear treasury projection causal sobre owners existentes;
8. integrar projection en reporting/policy sólo donde la authority sea suficiente;
9. añadir tests adversariales de duplicate, pending/unknown, restart, local persistence failure, injected reconciliation, free-capital UNKNOWN y escenarios P-030;
10. reauditar, CI branch, PR, exact-main y reconciliación canónica antes de HECHO.

## Validación requerida

- schema/migration compatibility y restart;
- duplicate logical effect no second dispatch;
- same durable idempotency key tras recovery;
- pending/submitted no materializa transaction;
- explicit rejection no materializa transaction;
- injected settled observation materializa exactamente una vez;
- external settled + local failure queda reconciliation-required;
- UNKNOWN balance no se convierte en 0/free capital;
- task commitments reducen free capital sin convertirse en realized cost;
- internal allocation/return no se convierte en external P&L;
- justified extraordinary spend no es bloqueado por guard universal 50%;
- no creator approval por amount-only;
- prediction/outcome learning permanece en P-023 authorities;
- full tests/security/typecheck/build/Windows/public distribution CI;
- LIVE sólo bajo P-005 con funds/authorization reales.

## Evidencia hasta el gate

- main exacto `d14f04b9fdea98eecd9d8e772b223b55d1fcf010` sigue vigente al gate;
- CI main `36220600132` SUCCESS 8/8;
- branch audit checkpoint `dac8d555d0d97cd023c4692c8fc1731de26bf3a5` estaba 4 commits ahead / 0 behind y sólo contenía ProjectOps;
- product source permaneció sin cambios durante la auditoría;
- findings derivados de source actual: `src/agent/tools-core.ts`, `src/conway/client.ts`, `src/conway/credits.ts`, `src/orchestration/simple-tracker.ts`, `src/orchestration/orchestrator-core.ts`, `src/agent/policy-engine.ts`, `src/agent/policy-authorization.ts`, `src/agent/spend-tracker.ts`, `src/survival/funding.ts`, `src/state/schema.ts`, `src/economics/child-economics.ts`, P-003/P-023/P-025/P-030 y tests financieros/económicos existentes.

## Siguiente punto verificable

Implementar la unidad 1 — narrow financial-effect state + migration — sin alterar todavía business P&L ni crear una segunda accounting authority; después reauditar schema/migration/owners antes de conectar side effects.