# P-003 — Reconciliar e integrar child capital semantics de PR #29

State: PARCIAL
Priority: ALTA

Required-Context:
- `AGENTS.md`
- `ProjectOps/PROJECT.md`
- `ProjectOps/CONTINUITY.md`
- `ProjectOps/PLAN.md`
- `src/state/schema.ts`
- `src/replication/`
- `src/orchestration/`
- `src/agent/`

External-Evidence:
- GitHub PR #29 — `feat: make child capital semantics explicit before profitability decisions`
- Branch: `abos/child-capital-semantics-v1`
- Observed head at P-001 audit: `423812c56c70d31451075232fe2c8a4d848a7ee5`

## Objetivo

Reauditar, reconciliar e integrar correctamente el trabajo existente de PR #29 contra el HEAD actual de `main`, sin reemplazarlo por una implementación paralela.

## Problema que resuelve

ABOS necesita distinguir de forma causal:
- parent funding histórico;
- child live balance;
- internal capital allocation/return;
- realized task cost;
- estimated commitments;
- attributed revenue;
- external P&L;
- profitability;
- ROI.

El estado actual de `main` no debe inventar esas equivalencias.

## Semántica obligatoria

- unknown != zero;
- unknown profitability != unprofitable;
- funding != balance;
- funding != expense;
- internal capital flow != external P&L;
- realized cost != estimate/commitment;
- ROI no existe sin numerator/denominator válidos;
- parent bookkeeping no sustituye live child observation.

## Estado actual

PARCIAL porque existe source en PR #29 y su descripción registra push CI verde histórico, pero:
- PR sigue abierto;
- no está integrado en `main`;
- su base antecede commits posteriores;
- debe reauditarse después del cutover ProjectOps.

## Auditoría previa obligatoria

1. comparar PR head con `main` actual;
2. revisar si `TransactionType`, reports, funding protocol o schema cambiaron después de su base;
3. confirmar que no existe ya una implementación equivalente merged;
4. seguir todos los productores/consumidores de child funding/cost/reporting;
5. verificar units y migration/backward compatibility;
6. revisar que no se active una decisión automática de kill/fund/recall usando inputs unknown;
7. ejecutar tests focalizados + full CI aplicable.

## Fuera de alcance

Este P no obliga a inventar una autoridad de live child revenue/balance si no existe. Mantener `unknown` es correcto.

Tampoco autoriza auto-profitability policies posteriores sin evidencia suficiente.

## Criterios de aceptación

- PR reconciliado contra HEAD actual;
- diff no duplica una autoridad existente;
- capital allocation y external P&L separados;
- unknown preservado;
- tests/CI actuales PASS;
- merge integrado en main;
- ProjectOps actualizado con SHA/evidencia.

## Definición de HECHO

Solo HECHO después de integración real en `main` y validación actualizada. CI histórico del branch no basta.
