# P-008 — Alinear Runtime Truth y capability claims con evidencia real

State: EN_EJECUCIÓN
Priority: CRÍTICA

Required-Context:
- `AGENTS.md`
- `ProjectOps/PROJECT.md`
- `src/agent/`
- `src/capabilities/`
- `src/memory/`
- `src/self-mod/`
- `src/setup/`
- `src/types.ts`

Dependencies: P-006, P-003; ambas satisfechas. Coordina estrechamente con P-009..P-018.

## Objetivo

Crear un contrato único para que prompts, planner, UI/CLI, children y runtime nunca presenten una capacidad como disponible sólo porque existe código, un paquete instalado, una descripción o un test aislado.

## Estado observado

Intervención activa sobre `abos/p008-runtime-truth-v1`, abierta desde `main` `fed2fe3c836dfc4351934a4abedf78898782728e`.

Estado técnico actual: **SOURCE_GREEN / FINAL_RECONCILIATION_SELF_VALIDATES_TO_INTEGRATION_READY**.

La auditoría y ejecución confirmaron:
- `CapabilityDescriptor.available` era insuficiente como authority por sí solo;
- `CapabilityRegistry` ya es la authority genérica adecuada y debe EXTEND/UNIFY, no duplicarse;
- `src/environments/` posee evidence/availability/observedAt reutilizable y no debe convertirse en segunda generic authority;
- tool definition, installed/enabled y skill enabled son inventario/discovery, no readiness;
- `CapabilityResolver.use_existing` sólo acepta `VERIFIED_AVAILABLE` con evidence temporal suficiente;
- restart de skills revalida requisitos actuales y no reactiva inventario stale;
- system prompt, planner y status/list distinguen soporte arquitectónico, inventory y runtime availability;
- `AbosDatabase.getInstalledTools()` sí filtra `enabled = 1`; la hipótesis preliminar contraria quedó falsada;
- installed MCP era una ruta material de false-success y ya quedó neutralizada sin implementar prematuramente P-015;
- P-015 sigue siendo la authority de implementación MCP real.

## Arquitectura objetivo

Toda capability observable puede expresarse al menos como semántica equivalente a: DISCOVERED_UNVERIFIED, ACQUIRED, PROBED, VERIFIED_AVAILABLE, DEGRADED, UNAVAILABLE, UNAUTHORIZED, PROHIBITED o UNKNOWN. El modelo permanece extensible y de mundo abierto.

`CapabilityRegistry` permanece como authority genérica única en memoria. `available` legacy sobrevive únicamente como proyección derivada de estado/evidence más ricos.

Los producers evidence-rich, en particular environment providers, proyectan availability/evidence/observedAt al registry mediante `registerEnvironmentSnapshot()`. Los consumers operativos sólo elevan `use_existing` desde evidencia actualmente suficiente.

Prompt/status/list distinguen explícitamente:
- soporte arquitectónico;
- definición cargada;
- instalación/configuración;
- autorización;
- probe/observation;
- `VERIFIED_AVAILABLE`.

## Invariantes

- source/test/install != capability success;
- UNKNOWN no se promociona por optimismo;
- una capability puede degradarse/retirarse sin borrar historia;
- amplitud no autoriza claims falsos;
- no crear un segundo CapabilityRegistry;
- no cerrar el mundo de providers/capabilities artificialmente;
- availability derivada conserva evidence/authority/temporalidad suficientes;
- `VERIFIED_AVAILABLE` requiere evidence no vacía y observación temporal válida;
- restart no promueve availability por pérdida de evidence fresca;
- configuración MCP no equivale a ejecución MCP;
- una ruta MCP sin adapter real no puede devolver éxito aparente;
- P-008 no implementa prematuramente P-014 ni P-015.

## Hipótesis — resolución vigente

H1 — CONFIRMADA. El booleano `available` y la promoción por existencia eran causa material de overclaim.

H2 — PARCIALMENTE FALSADA / REUTILIZADA. No existe otro lifecycle genérico canónico que deba sustituir `CapabilityRegistry`; sí existe evidence rica en `environments` que debe proyectarse al registry.

H3 — CONFIRMADA. `available` puede mantenerse como compatibilidad derivada, nunca como authority independiente.

H4 — CONFIRMADA PARCIALMENTE. Producers que realmente inspeccionan/proban pueden elevar disponibilidad con provenance; inventario/tool definition/enabled por sí solos no.

H5 — FALSADA. `loadInstalledTools()` no carga filas disabled: `getInstalledTools()` ya aplica `WHERE enabled = 1`. El defecto restante era `enabled/installed != verified readiness`.

H6 — CONFIRMADA. El executor MCP nominal constituía false-success; debía cerrarse sin construir el runtime MCP de P-015.

## Decisión técnica

Clasificación dominante: **EXTEND + UNIFY + CORRECT**, no CREATE.

1. Extender descriptor lifecycle/evidence conservando compatibilidad.
2. Normalizar todo registro en `CapabilityRegistry`.
3. Requerir evidence + `observedAt` válido antes de aceptar `verified_available`.
4. Resolver sólo consume verified evidence para `use_existing` y preserva UNAUTHORIZED/PROHIBITED/UNKNOWN.
5. Proyectar environment observations con authority/evidence/temporalidad.
6. Tratar tool/skill existence como discovered/unverified salvo producer evidence-rich.
7. Revalidar persisted skills al restart.
8. Corregir claims del system prompt/planner/status para separar architectural support / loaded inventory de runtime readiness.
9. Mantener MCP configurado como inventory unverified y no inference-callable hasta que P-015 entregue un adapter real/verificado.
10. Preservar fail-closed defense-in-depth si una ruta intenta ejecutar MCP nominal directamente.

## Implementación alcanzada

Ya implementado en la rama activa:
- lifecycle/evidence helpers en `src/capabilities/model.ts`;
- normalización/proyección environment en `src/capabilities/registry.ts`;
- resolución evidence-backed en `src/capabilities/resolver.ts`;
- `resolve_capability` con provenance/state explícitos;
- restart revalidation en `src/skills/loader.ts`;
- system prompt corregido para no afirmar possession/readiness por arquitectura o carga de tool definitions;
- planner corregido para no convertir roles/capabilities descritas en disponibilidad verificada;
- status/list surfaces principales usan semántica de inventory cuando sólo existe inventory;
- runtime-truth policy niega la ruta nominal MCP sin adapter verificado;
- built-in MCP persiste `runtimeTruth: configured_unverified` y `enabled:false`;
- `loadInstalledTools()` excluye MCP nominal de superficies inference-callable;
- executor MCP nominal falla cerrado en vez de devolver texto de éxito;
- agent loop y orchestrator proyectan environment snapshots con `registerEnvironmentSnapshot()` para conservar evidence/observedAt.

## Validación observada

### Regresiones útiles

Head `67d09828e1866bd264d9de67627c80fcaa0a6338`:
- CI detectó un fixture que pretendía `verified_available` con evidence pero sin `observedAt`.
- Se corrigió el test; source permaneció estricto.

Head `34080a08c31f943461b7a305d756ee53126a1b92`:
- CI `34795241873`: FAIL source/test.
- 109 test files PASS / 1 FAIL.
- 1907 tests PASS / 3 FAIL de 1910.
- Los 3 fallos correspondían a MCP/list runtime truth y quedaron corregidos.

### Validación dirigida final

Run `34795993901`:
- 10 reemplazos guardados/exactos PASS;
- `pnpm run typecheck` PASS;
- 9/9 test files PASS;
- 32/32 tests PASS;
- commit/push PASS.

Source commit: `1678f6fed32e634f963b05af16708597cdd1a615`.

### Branch validation completa

Commit humano `f41d5eddbe8b80e6588a5be282f9458281fa710d`:
- CI `34796248044`: SUCCESS;
- ProjectOps Integrity `34796248021`: SUCCESS;
- Node 22 build/test/security: SUCCESS;
- Node 24 build/test/security: SUCCESS;
- Windows regression 22/24: SUCCESS;
- public distribution smoke 22/24: SUCCESS;
- security/dependency audit: SUCCESS;
- rebrand integrity: SUCCESS.

Compare `main fed2fe3c...` → `f41d5edd...`: ahead 47, behind 0, 30 net changed files, sin drift lateral material encontrado.

PR #34 sobre ese head: OPEN / DRAFT / mergeable=true; sin reviews ni review threads abiertos.

## Gate final de integración

El commit que contiene esta reconciliación final no cambia source. P-008 pasa canónicamente a **INTEGRATION_READY** sin requerir otro commit cuando ese exact-head satisfaga simultáneamente:
1. CI = SUCCESS;
2. ProjectOps Integrity = SUCCESS;
3. PR #34 sigue apuntando exactamente a ese head, mergeable y sin objeción material abierta.

Si alguna condición falla, P-008 permanece EN_EJECUCIÓN y se diagnostica el fallo antes de otro intento.

## Trabajo restante antes de HECHO

Una vez satisfecho el gate final:
1. actualizar body/metadata de PR #34 con evidencia vigente;
2. marcar PR ready for review;
3. mergear con expected exact-head;
4. ejecutar/observar CI + ProjectOps sobre `main` integrado;
5. sólo después elevar P-008 a HECHO y rotar continuidad según `PLAN.md`.

## Failure/rollback

Migración de estados acepta registros legacy y conserva UNKNOWN. Rollback no puede convertir registros nuevos en disponibilidad ficticia. Cualquier proyección `available` backward-compatible deriva de evidence explícita.

Los cambios de prompt/list/MCP son semánticos, no una reducción arbitraria de capacidades: una capability realmente disponible sigue pudiendo usarse; lo eliminado es el claim o éxito anticipado sin evidencia.

## Definition of Done

No existe ruta material identificada donde ABOS anuncie una capacidad ejecutable sin authority/evidence correspondiente; tests adversariales demuestran la separación entre discovered/installed/probed/verified y degradation/restart no elevan claims ficticios.

La Definition of Done técnica de rama está demostrada sobre `f41d5edd...`, pero P-008 permanece EN_EJECUCIÓN hasta que esta reconciliación final se auto-valide y, después, integración + validación de `main` completen el cierre.
