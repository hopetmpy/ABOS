# P-009 — Corregir Authority, Provenance y trust boundaries end-to-end

State: EN_EJECUCIÓN
Priority: CRÍTICA

Required-Context:
- `AGENTS.md`
- `ProjectOps/PROJECT.md`
- `constitution.md`
- `src/agent/`
- `src/replication/`
- `src/registry/`
- `src/state/`

Dependencies: P-006; satisfecha. Debe preceder P-010, P-014..P-018, P-028 y P-030.

Activation-Baseline:
- `main` exacto: `1a2a548276ec47289c77b2085c5c939c6bca0019`
- P-008: HECHO, integrado por PR #34 y revalidado sobre ese `main`
- Rama activa: `abos/p009-authority-provenance-v1`
- Segmento activo: `ProjectOps/continuity/C0005.md`

## Objetivo

Preservar el origen y autoridad real de creator, agent, child, external user/service y transport identity desde ingress hasta policy/tool execution, evitando que contenido externo gane autoridad por relabeling, parsing o forwarding interno.

## Riesgo observado

La auditoría P-009 confirmó dos rutas materiales de pérdida de provenance:
- inbox Social conversacional era relabelado a `source="agent"` antes de policy, aunque ABOS no verificaba criptográficamente el sender inbound;
- Colony autorizaba parent/child a partir del `message.from` interno sin ligarlo primero al sender observado por el transporte persistido.

La evidencia y resolución detallada viven en `ProjectOps/continuity/C0005.md`.

## Arquitectura objetivo

Un envelope de provenance debe separar: asserted actor, authenticated/observed transport principal, origin class, delegation chain, parent/child relation, authorization evidence y transformation history cuando sea material. Policy consume autoridad derivada; nunca confía ciegamente en un campo de payload controlable por el remitente.

La arquitectura final debe reutilizar o extender autoridades existentes siempre que sea posible. P-009 no autoriza crear un segundo identity registry, policy authority, message bus, child authority ni persistence source sólo para transportar provenance.

## Invariantes

- contenido no puede elevar su propia autoridad;
- parent != child; transport != asserted from;
- forwarding conserva provenance;
- identity unknown permanece unknown/untrusted;
- no almacenar secretos innecesarios en eventos públicos;
- un campo `from`/`source` controlable por payload no acredita autenticación;
- legacy ambiguity debe degradar autoridad, no inventarla;
- constitution/policy/auth siguen siendo fronteras separadas;
- source/CI no se eleva a autenticación LIVE que no fue ejercitada.

## Interrogación inicial obligatoria

Antes de modificar source:
1. localizar todos los ingress de mensajes/comandos/eventos externos, internos, parent/child y registry;
2. identificar qué transport principal puede observar realmente cada ingress;
3. seguir asserted sender/source/origin desde parseo hasta persistencia, forwarding, planner, policy y tool execution;
4. buscar tipos/envelopes/metadata equivalentes antes de crear uno nuevo;
5. determinar qué parte es authority y qué parte es sólo claim del payload;
6. reproducir spoofed `from`, external→agent relabel y al menos una ruta de forwarding material;
7. contrastar una hipótesis alternativa: el relabel puede ser sólo representación interna sin producir privilege elevation si policy ignora ese campo;
8. inspeccionar schema/migrations y backward compatibility antes de cualquier cambio persistente;
9. definir rollback/migration y nivel de evidencia requerido;
10. alcanzar `DECISION_READY` antes de tocar source productivo.

Estado de la puerta: **COMPLETADA** antes del commit productivo. Decisión registrada en C0005 como `EXTEND + UNIFY + CORRECT + MIGRATE`.

## Ataques/pruebas requeridos

Spoofed `from`, external→agent relabel, nested forwarding, replay, stale delegation, child impersonation, malformed envelope, restart/persistence y backward compatibility.

Cuando corresponda, cada test debe distinguir:
- identidad afirmada;
- identidad observada/autenticada por transporte;
- autoridad derivada;
- decisión de policy;
- actor/executor que finalmente ejecuta.

## Hipótesis de apertura — resolución

H1 — **CONFIRMADA**: existía una ruta donde payload/source lógico externo se relabelaba como agent-originated y policy le atribuía mayor autoridad.

H2 — **FALSADA para inbox conversacional**: el relabel no era sólo metadata; `inputSource` alimenta directamente la derivación de authority y reglas de policy.

H3 — **CONFIRMADA**: persistencia v14 perdía provenance y Colony reconstruía autoridad desde un campo interno insuficiente.

H4 — **CONFIRMADA**: existían contratos parciales que debían REUSE/EXTEND/UNIFY; no se creó un segundo identity/provenance authority.

## Implementación

Source head productivo: `d5976ee6a89ba13e552cf95842fca410a87fccf8`.

Resultado implementado:
- `InputSource` incorpora `external` y la authority existente lo trata como external;
- `InboxMessage`/DB v15 conservan `transport`, `senderVerification` y `transportSender`;
- Social inbound queda `social_relay / relay_asserted`, sin inventar crypto verification;
- LocalDB internal queda `local_db / local_trusted`;
- legacy rows migran a `legacy_unknown / unknown`;
- `AgentTurn.inputProvenance` conserva evidencia y transformaciones relevantes;
- inbox sólo deriva `agent` si todo el lote es `local_db/local_trusted`; social/legacy deriva `external`;
- Colony compara inner `from/to` con outer sender/recipient persistido antes de cualquier handler/parent authorization;
- `verifyMessageSignature()` existente en `src/social/protocol.ts` permanece como verificador canónico para una futura evidencia inbound que realmente incluya firma verificable.

## Evidencia de implementación

- Primer run one-shot `34799157518`: FAIL controlado por fixture manual v14; patch y typecheck PASS, nuevas pruebas adversariales PASS; no produjo commit source.
- Segundo run one-shot `34799290285`: SUCCESS sobre Node `22.23.2`.
- Typecheck: PASS.
- Targeted tests: **6/6 files PASS, 159/159 tests PASS**.
- `authority-provenance.test.ts`: 7/7 PASS.
- Commit source: `d5976ee6a89ba13e552cf95842fca410a87fccf8`.
- Helpers one-shot: eliminados del head productivo.
- Diff `d0560f41...` → `d5976ee6...`: ahead 3, behind 0, 10 archivos netos y sin drift lateral detectado.

El commit source fue creado por `github-actions[bot]`; sus gates estándar posteriores quedaron `action_required` antes de runner por política de actor. Se clasifica `BLOCKED_PRE_RUNNER / ACTOR_APPROVAL_POLICY / NO VERIFICADO`, no source FAIL. La reconciliación humana posterior debe obtener CI completo sobre un SHA exacto estable.

## Rollback

Cambios de schema son aditivos y no destruyen mensajes históricos; ante ambigüedad legacy, la autoridad degrada a menor trust compatible en lugar de inventar autenticación.

Un rollback no puede volver a promover un payload claim como principal autenticado. V15 conserva metadata suficiente para distinguir legacy/unknown de observed/relay-asserted/local-trusted.

## Evidencia requerida para cierre

Como mínimo E3/INTEGRATION_VERIFIED sobre el SHA final, con tests adversariales que recorran ingress→authority/policy→tool boundary y persistencia/restart cuando aplique. E5 sólo se reclama para transport/provider real si P-009 depende materialmente de una autenticación externa ejercitada; en el estado actual Social inbound permanece `relay_asserted`, por lo que esa frontera LIVE queda separada.

Pendiente:
- CI completo + ProjectOps Integrity sobre el head humano de rama;
- PR #35 exact-head, mergeable y sin objeción material;
- merge protegido;
- CI + ProjectOps sobre `main` integrado;
- sólo entonces P-009 puede pasar a HECHO.

## Definition of Done

Cada acción sensible puede explicar quién la solicitó, qué identidad se observó, qué delegación existe y por qué policy le otorgó ese nivel; tests de spoofing no logran elevación.

Además:
- no existe segunda authority de identity/provenance creada por conveniencia;
- forwarding/persistence conservan provenance suficiente;
- legacy unknown no se promociona;
- migrations/backward compatibility están validadas si se modificó schema;
- branch/PR/main quedan integrados y revalidados;
- `CONTINUITY.md` y este módulo reflejan evidencia real.
