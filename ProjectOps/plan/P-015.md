# P-015 — Sustituir MCP nominal por un runtime MCP real y verificable

State: EN_EJECUCIÓN
Priority: CRÍTICA

Required-Context:
- `AGENTS.md`
- `src/agent/tools.ts`
- `src/capabilities/`
- `src/self-mod/`
- `src/state/`
- `package.json`

Dependencies: P-014, P-009, P-010, P-013. P-017 reutiliza este runtime para adquisición.

Decision-State: DECISION_READY
Evidence-State: DECISION_READY / SOURCE_UNMODIFIED
Activation-Baseline:
- P-014 product PR: #44
- P-014 product merge: `bf9adfd11617a37698ce7095248c1e1228f0d3cf`
- P-014→P-015 transition PR: #45
- canonical baseline `main`: `87e256a93a655b1ccce000cd2de3896a8f3f74b5`
- baseline CI `35418524268`: SUCCESS
- baseline ProjectOps `35418524330`: SUCCESS
- ancestry-reconciled branch head: `622087e9e007c2922020ebd98bee14618f843d14`
- branch CI `35418659669`: SUCCESS
- branch ProjectOps `35418659610`: SUCCESS
- working branch: `abos/p015-mcp-runtime`
- active segment: `ProjectOps/continuity/C0011.md`
- product source P-015 modified at activation: NO

## Objetivo

Hacer que un servidor MCP sea una mano real: descubrir/establecer transporte compatible, obtener capabilities/schemas, ejecutar calls reales, observar resultados/errores, verificar funcionalidad y registrar evidencia en Capability Fabric.

## Estado observado

La auditoría encontró una ruta donde instalar/registrar un MCP no demuestra un handshake/call MCP real y un executor puede devolver texto nominal. El objetivo no es eliminar MCP, sino completar la capacidad.

## Arquitectura objetivo

MCP adapter encapsula transports soportados por la versión vigente del protocolo al ejecutar este P. Debe existir lifecycle separado entre discovered, configured, connected/discovered, probed, verified, degraded/unavailable. `tools/list`/equivalente y call reales se transforman a descriptors del P-014 sin hacer de MCP una autoridad paralela.

## Reglas de actualidad

La especificación MCP puede cambiar. Antes de implementar, verificar documentación oficial/SDK vigentes; no codificar desde recuerdos de esta planificación. Preferir SDK/protocolo oficial compatible con Node soportado cuando reduzca riesgo.

## Security/authority

- servidor local/remoto conserva provenance;
- auth/credentials no se guardan en ProjectOps;
- tool effects/permissions se policy-checkean antes de call;
- supply-chain y endpoint trust se evalúan antes de activation;
- un servidor no puede autodeclararse trusted/verified.

## Failure matrix

Transport crash, malformed schema, server hangs, auth expired, partial output, side effect timeout, reconnect/restart, tool list changes, duplicate tool names, protocol/version incompatibility.

## Validación

Fake deterministic server para protocol tests + al menos un servidor MCP real autorizado para integration cuando sea posible; call real con input/output schema; disconnect/reconnect; negative auth; timeout/cancel; restart.

## Definition of Done

ABOS puede conectar a un MCP, descubrir al menos una tool real, policy-checkearla, llamarla, validar su respuesta y registrar la capability como verified sólo después del probe/call correspondiente.

## Activación P-015

P-015 se activa únicamente para auditoría. Antes de cualquier modificación de producto debe verificarse la especificación/SDK MCP oficial vigente, mapear la implementación nominal existente, productores/consumidores, trust/auth/policy/restart y alcanzar `DECISION_READY`. Esta transición no acredita handshake/call MCP real ni evidencia LIVE.


## Auditoría y decisión — DECISION_READY

La implementación actual es deliberadamente nominal/fail-closed: configuración MCP persiste `configured_unverified`, queda runtime-disabled y no existe cliente/protocolo MCP real. El inventario enabled-only no permite enumerar esas configuraciones por la API canónica; Capability Fabric P-014 y Policy/P-013 ya son authorities reutilizables. Además, dynamic MCP outputs no caben en la allowlist nominal de sanitización y los nombres pueden colisionar con `tools.find()` first-match.

Se verificó antes de implementar la revisión oficial MCP `2026-07-28` y el SDK TypeScript v2 estable. Node 22/24 de ABOS es compatible. La decisión es **`OFFICIAL_SDK_V2 / DEDICATED_MCP_ADAPTER / EXISTING_AUTHORITIES`**, no JSON-RPC propio, passthrough del proveedor ni habilitar inventario nominal.

Primera unidad: **`MCP_CORE_STDIO`** — inventory API aditiva, SDK client core con negociación auto, stdio discovery/call real, mapping collision-safe hacia `AbosTool`/CapabilityDescriptor, Policy central, sanitización de output, lifecycle evidence-backed y servidor determinista de test. Streamable HTTP/auth/reconnect avanzado queda como siguiente unidad del mismo adapter.

Product source modificado al alcanzar `DECISION_READY`: **NO**.
