# P-017 — Implementar discovery, acquisition, composition y construction de capacidades

State: HECHO
Priority: ALTA
Technical-State: HECHO / E3_MAIN_GREEN / INTEGRATION_VERIFIED

Required-Context:
- `AGENTS.md`
- `src/capabilities/`
- `src/self-mod/`
- `src/skills/`
- `src/agent/tools.ts`
- `src/environments/`
- `package.json`

Dependencies: P-012, P-014, P-015, P-016, P-013.

## Objetivo

Cuando ABOS detecte un capability gap, permitir una búsqueda adaptativa y autorizada: reutilizar → descubrir → adquirir → componer → construir → probar → registrar, en vez de estar limitado a tools preinstaladas o instalar ciegamente paquetes.

## Fuentes candidatas abiertas

MCP, CLI, API/SDK, package Node/Python u otro runtime disponible, OS capability, service externo, skill existente o adapter construido por ABOS. Esta lista es ejemplo, no allowlist exhaustiva.

## Acquisition lifecycle

NEED → CANDIDATES → provenance/risk/cost evaluation → ACQUIRE in quarantine/staging → configure without leaking secrets → functional probe → register VERIFIED or mark DEGRADED/REJECTED → observe real use → retain/upgrade/retire.

## Reglas

- preferir capacidad existente/estándar antes de escribir otra;
- package installed != capability usable;
- no descargar/ejecutar código no confiable sin policy/supply-chain evaluation;
- adquisición con coste requiere treasury/authorization;
- construction usa P-012 transactional self-mod;
- cada intento fallido reduce espacio de búsqueda.

## Validation

Escenario E2E donde una tarea requiere capability ausente: ABOS detecta gap, descubre candidato, lo adquiere o construye, hace probe, lo registra, lo usa y verifica outcome. Escenarios negativos: paquete malicioso/incompatible, auth ausente, coste excedido, probe falla.

## Definition of Done

Un capability gap legítimo no obliga a hardcode manual: existe un pipeline reusable y auditable que puede terminar en VERIFIED, UNAUTHORIZED, UNAVAILABLE o NO_DISCOVERED sin inventar éxito.

## Decisión e implementación

Decision-Class: `EXTEND / REUSE / UNIFY`.

P-017 se implementó como orchestration dentro de `src/capabilities/` preservando ownership existente:
- P-014 conserva readiness/lifecycle authority;
- P-013 conserva Evidence Fabric;
- P-015 conserva MCP runtime;
- P-012 conserva construction/self-mod transaccional;
- `remediate_capability` coordina discovery/remediation y vuelve a resolver el contrato original;
- nested effects reingresan por Policy mediante protected tool invocation;
- provenance/assessment incompletos fallan cerrado;
- install/config/build success no se promociona a VERIFIED por sí solo;
- composition persiste un plan declarativo, exige dependencies verificadas y revalida current component contract coverage antes de cada ejecución;
- external/dynamic tool names no pueden shadowear silenciosamente las tools internas ABOS;
- nested sensitive arguments se redactan de durable outer records.

## Evidencia final

- baseline canónico: `08b7a5cce8f18c57c52e6a2f43048b6f1c214e50`;
- branch: `abos/p017-capability-acquisition`;
- hardening source: `7e74e26ef356338a6a5359d43391ef6106c42836`;
- regression head: `6f6bd342ecaddaa1ca55ca4e8cdbf40bd0b8957c`;
- branch CI `35903430155`: SUCCESS, 8/8;
- branch ProjectOps `35903430064`: SUCCESS;
- PR #51 exact-head CI `35903990411`: SUCCESS, 8/8;
- PR #51 ProjectOps `35903990471`: SUCCESS;
- squash merge: `c5c5ae856923ba74c943cc26beca26ea38de0dcc`;
- exact-main CI `35904429871`: SUCCESS, 8/8;
- exact-main ProjectOps `35904429875`: SUCCESS;
- Windows22 timeout previo en `35817655824` fue no reproducido por rerun exacto `107323769559` SUCCESS sin modificar P-016.

P-017 queda HECHO en E3 sobre `main`. Esto no acredita E5/LIVE externo.