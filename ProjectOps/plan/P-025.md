# P-025 — Evolucionar Strategic Cognition, Plan Review y Adaptive Path v2

State: HECHO
Priority: CRÍTICA
Dynamic-State-Authority: `ProjectOps/CONTINUITY.md`
Execution-Scheduler: `AGENTS.md`

Este módulo conserva el **blueprint técnico, alcance, invariantes, preguntas de auditoría, validaciones y Definition of Done** de P-025.

El estado operativo vivo, la rama/HEAD de reanudación, hipótesis ya resueltas, `DECISION_READY`, bloqueos y el **siguiente punto verificable** se resuelven exclusivamente desde `ProjectOps/CONTINUITY.md`, su `Active-Segment`, Git/source/runtime/tests y evidencia actual. Cualquier observación histórica conservada aquí es contexto de diseño, **no una instrucción de reanudación ni una segunda authority de ejecución**.

Required-Context:
- `AGENTS.md`
- `ProjectOps/system/ABOS_ADAPTIVE_REASONING_LAYER.md`
- `docs/ADAPTIVE_PATH_INTELLIGENCE.md`
- `src/orchestration/`
- `src/intelligence/`
- `src/capabilities/`
- `src/environments/`
- `src/agent/`

Dependencies: P-009, P-010, P-013, P-014, P-018, P-022, P-023, P-024.

## Objetivo

Reemplazar review/consensus nominal y planning excesivamente lexical por un ciclo estratégico basado en objetivos, compromisos, alternatives, beliefs, evidence, expected value, uncertainty, capabilities, environment, cost, contingencies y failure recovery, preservando Adaptive Path como autoridad de rutas.

P-025 debe convertir el principio `CAPABILITY_PRESERVATION_BEFORE_RESTRICTION` en juicio operativo general: cuando una capacidad legítima presenta riesgo, ABOS debe intentar gobernarla mediante mejor razonamiento y evidencia antes de eliminarla o escalarla innecesariamente a un humano.

## Frontera técnica a auditar

La baseline histórica que motivó P-025 mostró dos clases de riesgo que deben contrastarse siempre contra source/runtime actual antes de modificar:

- paths de `plan-mode` donde auto/consensus puedan aprobar de forma placeholder o sin review material;
- el caso inverso donde un riesgo gobernable pueda terminar convertido en restricción/creator approval aunque la capability siga siendo legítima.

Estos puntos son **preguntas de auditoría del módulo**, no estado vivo. Si CONTINUITY/Git ya demuestran que una de ellas fue resuelta, no se reabre por ceremonia.

## Cognitive protocol objetivo

**DEFINE objective/commitment → decompose required outcomes → identify available/missing resources → interrogate assumptions → generate materially distinct paths → pre-mortem (“si falla, ¿por qué?”) → estimate evidence/value/cost/risk/liquidity/contingency → simulate/probe cuando aporte información → choose → execute one explicit boundary → observe → compare prediction vs outcome → update world model → replan.**

La decisión no se limita a “¿puedo hacer X?”. Debe incluir, cuando aplique:
- qué más hace falta para cumplir completamente el compromiso;
- qué ya existe y puede reutilizarse;
- qué debe adquirirse ahora y qué puede esperar;
- qué dependencia puede fallar y cuál sería la ruta de reemplazo;
- qué compromiso futuro cambia materialmente la decisión actual;
- qué evidencia justificaría gastar más, menos, esperar o cambiar de ruta.

## Review

Approval no puede ser constante. Review debe producir critique/evidence/conditions; consensus sólo existe si múltiples evaluadores/criterios reales lo sustentan, o se elimina ese claim. El usuario puede fijar modo manual/adaptive según riesgo.

Human review no es el fallback universal de una decisión difícil. Se escala cuando existe `REAL_BOUNDARY` o oversight manual explícito; `GOVERNABLE_RISK` debe resolverse autónomamente cuando ABOS posee autoridad y evidencia suficientes.

## Capability/Judgment gate

Antes de recomendar retirar, desactivar o human-gate una capability:
1. clasificar `REAL_BOUNDARY | GOVERNABLE_RISK | IMMATURE_CAPABILITY | REDUNDANT_OR_HARMFUL | UNKNOWN`;
2. evaluar al menos una ruta que preserve la capacidad;
3. demostrar por qué contexto/judgment/verificación/observabilidad/adaptation no bastan, si aun así se restringe;
4. registrar qué nueva información permitiría relajar una guard provisional.

## Adaptive Path invariants

Objective != method; equivalent failed path no se repite sin material change; auth/prohibition no se evade; capability gap usa P-017; environment failure scoped; UNKNOWN preservado; una desviación de coste/histórico es evidence para revisar, no una prohibición automática.

## Hipótesis de diseño a discriminar cuando sigan abiertas

- H0 `NO_CHANGE`: el runtime actual ya satisface materialmente P-025 y el plan quedó desactualizado.
- H1 `EXTEND_ADAPTIVE_PATH`: la authority correcta ya existe y sólo faltan semántica/wiring/review real.
- H2 `CORRECT_PLAN_MODE_REVIEW`: el defecto principal vive en orchestration/review y puede corregirse sin nueva authority.
- H3 `UNIFY_STRATEGIC_DECISION_FLOW`: existen rutas paralelas o nominales que deben converger sobre authorities ya canónicas.
- H4 `CREATE_NEW_STRATEGIC_AUTHORITY`: sólo válida si ninguna authority existente posee semánticamente la responsabilidad completa; no se presumirá.

La resolución actual de estas hipótesis pertenece a CONTINUITY/segmento activo. Este módulo no debe sobrescribir una resolución ya demostrada ni obligar a repetirla.

## Auditoría requerida

Cuando una de estas preguntas siga materialmente abierta según CONTINUITY/Git/evidencia:

1. leer el Required-Context material;
2. mapear producers/consumers de planning, review, consensus, Adaptive Path, beliefs/evidence, simulation, policy/capability/environment;
3. localizar tests y contratos que codifiquen autoapproval, consensus o human-gating;
4. contrastar historial relevante sólo donde cambie ownership/causalidad;
5. buscar evidencia falsadora para hipótesis todavía abiertas;
6. alcanzar el gate de decisión definido por `AGENTS.md` antes de una modificación material.

Si el Active-Segment ya acredita esos puntos, se continúa desde su siguiente punto verificable; **no se reinicia la auditoría por el texto de este módulo**.

## Ataques

Plan atractivo pero capability inexistente, estimate falso, ruta equivalente renombrada, infinite replanning, local optimum, approval circular, high-cost plan con low-cost probe disponible, stale belief, gasto históricamente anómalo con causa legítima, gasto pequeño sin valor, capability legítima eliminada por comodidad, creator approval usado para sustituir juicio interno.

## Validación

La implementación debe atacarse al menos contra:

- approval constante o consensus nominal;
- human-gating usado como sustituto del juicio cuando no existe `REAL_BOUNDARY`;
- plan ausente/corrupto con bypass silencioso;
- path/tasks materializados antes de review suficiente;
- capability/environment/simulation evidence omitida o interpretada fuera de su authority;
- path equivalente reintentado sin cambio material;
- residuos ejecutables después de reject/revise;
- restart, late-result, double execution e idempotencia donde sean materiales.

## Definition of Done

Una decisión compleja produce alternatives y motivos discriminantes, descompone el resultado comprometido en necesidades/dependencias, review real, pre-mortem/falsación proporcional al riesgo y replanning basado en evidence; distingue frontera real de riesgo gobernable; puede justificar causalmente una decisión extraordinaria sin depender de thresholds fijos; tests ya no codifican ni autoaprobación ficticia ni human-gating ficticio como PASS.

El estado `HECHO`, la evidencia de integración y el handoff no se declaran desde este módulo por sí solo: se reconcilian mediante `AGENTS.md` con CONTINUITY, Git/source/runtime/tests y los criterios anteriores.

## Evidencia de integración

P-025 cerró por PR #68. Final PR head `c40eac6ca4ff9e744d31d0331911b1a3d08fee4f`, CI `36100819999` SUCCESS 8/8; final main `8f57ae64971bbf369d5bcfc387a3352e97b91426`, exact-main CI `36101081385` SUCCESS 8/8. El detalle histórico y los findings de cierre están en `ProjectOps/continuity/C0021.md`.